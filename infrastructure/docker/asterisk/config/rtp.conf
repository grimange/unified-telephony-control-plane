[general]
; Canonical current reference-runtime RTP listener range.
rtpstart=10000
rtpend=20000

; Reclaim abandoned dialogs and RTP allocations without operator cleanup.
; These bounded values are intentionally longer than normal RTP jitter but
; short enough to contain failed external-media proof calls.
rtp_timeout=30
rtp_timeout_hold=30
