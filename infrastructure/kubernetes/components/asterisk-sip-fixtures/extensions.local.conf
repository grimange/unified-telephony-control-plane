[from-kamailio]
exten => c6-generic-proof,1,NoOp(UTCP C6 generic inbound proof fixture)
 same => n,Answer()
 same => n,Stasis(utcp-t0-observation,c6-generic-proof)
 same => n,Hangup()

exten => 9900,1,NoOp(UTCP local T3-S2A media fixture)
 same => n,Answer()
 same => n,Echo()
 same => n,Hangup()
