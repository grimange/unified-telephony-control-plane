apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: utcp-platform
  labels:
    app.kubernetes.io/part-of: utcp
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/part-of: utcp
    app.kubernetes.io/component: api
  ports:
    - name: php-fpm
      port: 9000
      targetPort: php-fpm
