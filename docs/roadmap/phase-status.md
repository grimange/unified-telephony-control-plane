# Phase Status

Current T3-S2C status: T3-S2A = Complete; T3-S2B = Complete; the FreeSWITCH runtime startup contract is corrected and the pinned image-level startup smoke is proven; PRODUCT_DEFECT-21 is corrected by composing the parity overlay on the complete canonical local overlay, preserving local images and generators; PRODUCT_DEFECT-22 is corrected by consuming verified browser credentials before generic application-runtime relay; the plain image-level FreeSWITCH INVITE smoke is proven; the FreeSWITCH parity overlay is safe and ready for live application and focused live proof; the provider-neutral contract is proven against Asterisk; runtime agnosticism is not yet proven until FreeSWITCH live parity passes; T3-S3 is Not Started; T3 is In Progress; `UTCP_PHASE=T1`. `PRODUCT_DEFECT-15 = closed`; `PRODUCT_DEFECT-16` is historical and open, with no workaround. `PRODUCT_DEFECT-17` through `PRODUCT_DEFECT-22` are corrected in the repository. Evidence: [`docs/evidence/t3/t3-s2c-freeswitch-invite-auth-correction.md`](../evidence/t3/t3-s2c-freeswitch-invite-auth-correction.md).

| Phase                                                        | Status      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
|--------------------------------------------------------------|-------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| F0 Repository contract and governance                        | Complete    | Repository governance, command convention, ADRs, and hygiene CI are established.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| F1 Minimal application skeleton                              | Complete    | Laravel API and Vue administration shell exist with health/version endpoints, tests, build checks, and local run documentation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| F2 Container build foundation                                | Complete    | Production-oriented API and web images build, containerized tests and smoke checks pass, and image metadata/runtime contracts are documented.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| F3 Docker Compose core platform                              | Complete    | Compose topology and proofs exist. After the local runtime authority cutoff, Compose is retained for disposable compatibility proof and explicit debug mode; `utcp-local` Kubernetes is canonical integrated local runtime.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| F4 CI quality baseline                                       | Complete    | GitHub Actions workflows, local CI-equivalent checks, dependency audits, workflow validation, container checks, and isolated Compose proof are established. Hosted GitHub Actions execution has not been observed for the uncommitted working tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| K0 Local k3d cluster foundation                              | Complete    | Repository-managed k3d/K3s cluster, local registry, kubeconfig isolation, namespaces, registry pull proof, recreate proof, and CI wiring are established. Hosted GitHub Actions execution has not been observed for the uncommitted working tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| K1 Kubernetes application base                               | Complete    | Kustomize-managed local Kubernetes manifests deploy PostgreSQL, Redis, migration Job, API, worker, scheduler, web, and internal gateway to `utcp-local`; port-forward, persistence, and restart proofs passed. Hosted GitHub Actions execution has not been observed for the uncommitted working tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| K2 Traefik and Gateway API                                   | Complete    | UTCP uses the one-cluster-at-a-time standard local edge on `127.0.0.1:80/443`; Gateway API, Traefik, TLS, route status, reserved-host rejection, and external runtime proof passed after controlled cluster recreation. APNTalk remained intentionally stopped and untouched. Hosted GitHub Actions execution has not been observed for the uncommitted working tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| K3 Kubernetes security boundaries                            | Complete    | Pod Security Admission is enforced as `restricted` `v1.35` on UTCP namespaces; default-deny NetworkPolicies and explicit allow paths are active; positive and negative connectivity proof, PSA rejection proof, K1/K2 regression proof, and cleanup proof passed locally. Hosted GitHub Actions execution has not been observed for the uncommitted working tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| K4 Kubernetes observability foundation                       | Complete    | Prometheus Operator, Prometheus, Alertmanager, Grafana, kube-state-metrics, Loki, and Alloy are installed in `utcp-observability` under restricted PSA and K3 NetworkPolicies; metrics, log ingestion, Grafana provisioning, synthetic alert delivery, persistence, gateway regression, and security regression proof passed locally. Hosted GitHub Actions execution has not been observed for the uncommitted working tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| C0 Control-plane application kernel                          | Complete    | Modular-monolith kernel primitives now exist for runtime-neutral operations, PostgreSQL leases and fencing, transactional outbox, inbox deduplication, idempotency, append-only audit, event envelopes, and architecture-boundary checks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| C1 Identity, tenancy, and authorization                      | Complete    | PostgreSQL-authoritative users, tenants, memberships, built-in roles/capabilities, first-party sessions, active-tenant selection, web-admin management, password lifecycle, bounded existing-user break-glass recovery, suspension behavior, C0 audit integration, API proof, browser proof, and Kubernetes migration proof are established locally. Hosted GitHub Actions execution has not been observed for the uncommitted working tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| C2 Runtime registry and runtime-node management              | Complete    | PostgreSQL-authoritative `RuntimeNode` registry, tenant ownership, normalized endpoints, encrypted write-only credentials, declared runtime capabilities, C1 authorization, C0 audit/idempotency/outbox integration, API proof, and repository Playwright browser proof are established locally. Hosted GitHub Actions execution has not been observed for the uncommitted working tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| C3 Command, event, projection, and reconciliation engine     | Complete    | Generic C3 engine code, PostgreSQL schema, Kubernetes process-role manifests, config checks, focused tests, proof harnesses, and runtime-engine status are established. The local runtime authority cutoff makes `utcp-local` Kubernetes the sole canonical integrated local runtime before C4 begins. Hosted GitHub Actions execution has not been observed for the uncommitted working tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| C4 Deterministic simulator adapter                           | Complete    | `simulator-deterministic` `RuntimeAdapter`, operation handlers, and event normalizer are registered with no branching in the generic C3 engine; the `simulator-event-source` Kubernetes process role runs with no public exposure; all seven scenarios (steady-ready, transient-failure-then-ready, terminal-failure, timeout-then-ready, duplicate-observation, disconnect-reconnect, configuration-drift-then-converge) are proven live against the deployed `utcp-local` workers with no manual dispatch; event-source and generic C3 worker restart/recovery are proven with no lost or duplicated work; bounded `simulator_*` metrics and alerts are live-proven. Two real C3 reconciliation defects (missing `SKIP LOCKED` fencing, and `last_operation_id` not preserved across non-`operation_required` results) were found under live concurrency and fixed with a regression test. Hosted GitHub Actions execution has not been observed for the uncommitted working tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| C5 Telephony-session and conference domain                   | Complete    | PostgreSQL-authoritative telephony sessions, conferences, runtime bindings, and conference participants are implemented with authenticated APIs, C1 authorization, C0 audit/idempotency/outbox integration, runtime-neutral conference operations, simulator execution, C3 raw receipts, normalized observations, projection checkpoints, automatic reconciliation, scheduler-driven expiry, bounded metrics, alerts, live Kubernetes conference lifecycle proof, restart/recovery proof, disposable Compose compatibility proof, and regression proof. Cross-tenant and stale-event edge cases are also covered by focused PostgreSQL tests. Hosted GitHub Actions execution has not been observed for the uncommitted working tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| T0 Asterisk ARI adapter                                      | Complete    | `asterisk-ari` adapter code, local internal Asterisk ARI fixture, listener role with persistent per-node claimed-connection ownership (fixed from a prior close/reopen-per-poll-cycle defect), listener-lease fencing, periodic HTTP liveness re-inspection keeping runtime evidence fresh (fixed from a prior connection-open-only observation defect that let a healthy node go falsely stale), stale-epoch cleanup on lease takeover (fixed from a prior duplicate-open-epoch defect), bounded exponential reconnect backoff reusing the existing per-node `reconnect_min_delay_ms`/`reconnect_max_delay_ms` configuration, C3 connection epochs, raw receipt ingestion, runtime-neutral readiness normalization/projection, RuntimeNode management surfaces, bounded metrics, alerts, and proof scripts are live-proven against the deployed `utcp-local` cluster: listener steady-state ownership, listener Pod restart/fenced takeover, internal Asterisk Pod restart/automatic recovery, authentication-failure sanitized evidence with bounded retry and reconciliation blocking, credential-generation recovery, the unsupported-conference-capability boundary, unknown-ARI-event safety, metrics/alert firing-and-resolution, and NetworkPolicy positive/negative connectivity (including a stale rendered Traefik-to-apiserver egress policy found and corrected via the existing canonical re-render/apply mechanism). `UTCP_PHASE` is now `T0`. Hosted GitHub Actions execution has not been observed for the uncommitted working tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| T1 Kamailio SIP-over-WSS signaling                           | Complete    | PostgreSQL-authoritative TelephonySession signaling credential issuance, HA1 storage, metadata-only readback, tenant-scoped API access, session-end revocation, and generic C3 event-source identity for RuntimeNode-backed and shared platform sources are proven. The T1-B registrar foundation live-proves pinned Kamailio WSS REGISTER, least-privilege authentication view/usrloc roles, trusted Gateway WSS routing, native usrloc persistence, refresh, replacement, explicit deregistration, session-end revocation, bounded runtime expiration, wrong-password and unsupported-algorithm rejection, and Kamailio restart recovery. The T1-C registration observer, projection, and reconciliation corridor is live-proven, including a fenced observer Pod takeover (kill the lease-holder, confirm the standby claims the lease and checkpoint continues advancing with no lost progress) performed during T1-G closure. The T1-D canonical User & Access Management UI, T1-E natural browser acceptance (real login, tenant selection, user/signaling detail, logout — no injected cookies or sessions), and a two-tenant non-platform-member isolation proof (two ordinary tenant-member accounts in two different tenants, created through the canonical admin API and a canonical temporary-password issuance/forced-change flow, each unable to see the other tenant's users or signaling data, and unable to reach the other tenant's user-detail route directly) are all live-proven. T1-F disposable Compose compatibility proof passed end to end (credential issuance, registrar, WSS, observer, projection, reconciliation, bounded expiry) with verified cleanup leaving no disposable container, network, volume, or secret file behind. Full K0-K4/C0-C5/T0 Kubernetes regression, security proof, observability proof, and full repository regression (tests, checks, builds, container smoke) passed. T1 is registration-only: it does not implement INVITE relay, dispatcher/runtime destination selection, application-dialog Record-Route, or SIP routes to Asterisk/RuntimeNodes. `UTCP_PHASE` is now `T1`. Hosted GitHub Actions execution has not been observed for the uncommitted working tree. See `docs/decisions/ADR-019-kamailio-signaling-registration-authority.md` and `docs/evidence/t1/kamailio-sip-over-wss-signaling.md`. |
| T2 Asterisk conference execution                             | Complete    | Asterisk conference execution is live-proven: canonical C5 conference and participant operations execute through the Asterisk adapter, ARI events drive normalized projection and reconciliation, retries remain idempotent, stale/wrong-node protections hold, and restart/recovery/cleanup corridors are documented in T2/T5 evidence. Browser media and Kamailio application-dialog routing remain T3/V0 scope, not T2 exit criteria. Hosted GitHub Actions execution has not been observed for the uncommitted working tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| T3 rtpengine browser media                                   | In Progress | T3-S1 repository foundation is implemented, `PRODUCT_DEFECT-1` and `PRODUCT_DEFECT-2` are both corrected and **confirmed resolved live**, `PRODUCT_DEFECT-3` is corrected and **confirmed closed live**, `PRODUCT_DEFECT-4` is corrected in the repository, and T3-S1 is ready for final scrape-discovery reproof. `PRODUCT_DEFECT-3` is corrected in `b21c117` and confirmed closed by the authorized-corridor reproof recorded in `docs/evidence/t3/t3-s1-rtpengine-foundation-live-proof.md`: the authorized Kamailio identity receives `result=pong`, the authorized Prometheus identity receives `HTTP/1.0 200 OK` from rtpengine `2224/TCP` with counters matching the bounded `40000-40099` range, unauthorized control and metrics remain denied, default-deny remains intact, and no canonical state or Kamailio runtime configuration changed. `PRODUCT_DEFECT-4` is corrected in `docs/evidence/t3/t3-s1-prometheus-operator-api-egress-correction.md`: the API-egress template now renders the exact `allow-prometheus-operator-apiserver-egress` policy selecting `app.kubernetes.io/component=prometheus-operator`, removes the ineffective unused `app.kubernetes.io/name=prometheus-operator` selector, preserves the pinned Kubernetes API destination and TCP port contract, and adds static rendered-label coverage validation for every observability API-dependent workload. The rtpengine `PodMonitor`, its selectors, its named `metrics` port resolving to TCP `2224`, and the Prometheus CR selectors remain unchanged and already verified correct. Only the final scrape-discovery live proof remains: operator readiness, PodMonitor job rendering, target health, and one ingested `rtpengine_*` sample. The resumed proof (`docs/evidence/t3/t3-s1-rtpengine-foundation-live-proof.md`, executed at `812c6ec`) proves: the final image identifies repository revision `812c6ec`, rtpengine `mr26.0.1.19`, upstream commit `3552ac76…`, `amd64`, user `1000:1000`, and no embedded credentials; local, registry, and **running** image digests all match `sha256:33cf7e2e…` (`linux/amd64` platform digest `sha256:ad8c7e02…`) on tag `0.1.0-k1-dev` with no `latest`; only rtpengine was rolled — no manifest was re-applied and all 34 other Pods retained their UID and restart count; startup succeeds with **no PID-file error** on `/run/rtpengine/rtpengine.pid` while `/tmp/rtpengine` is no longer required; restricted `v1.35` PSA admits the Pod with zero violation events; the effective security context captured from the **real running workload** matches ADR-020 §8 exactly (UID/GID `1000`, no capabilities, `NoNewPrivs`, `RuntimeDefault` seccomp, read-only root filesystem, no service-account token, no host namespaces, no HostPort, no HostPath); userspace forwarding (`--table=-1`), Pod-IP bind and advertisement, and the exact `40000–40099` range all hold; readiness validates a real `ng` `pong` about one second after container start and the EndpointSlice becomes Ready; a `SIGSTOP` on PID 1 makes the real `ng` liveness probe fail, kubelet restarts the container automatically (`exitCode 137`, restart count `0`→`1`, same Pod UID), and readiness, the PID file, and `ng` `pong` all return with the Deployment still at one replica; the ClusterIP UDP `2223` Service carries exactly one Ready endpoint equal to the current Pod IP; unauthorized control and unauthorized metrics are both denied with bounded failures, isolated through a `default`-namespace source with unrestricted egress; the media boundary is contained (no NodePort, LoadBalancer, Gateway/Ingress/UDPRoute, HostPort, k3d publication, node socket, or developer-host socket for `40000–40099`, with the application edge unchanged at TCP `80/443`); the metrics listener serves valid Prometheus text whose port counters match the bounded range exactly (`rtpengine_ports = 100`); relay unavailability produces a visible bounded failure with no Asterisk fallback and no canonical state change; restoration is fully automatic with no manual reconciliation, projection, or repair command; the live Kamailio configuration is byte-identical to git (`sha256 6e85abaf…`) with zero `rtpengine_offer`/`answer`/`manage`/`delete` or `rtpproxy` directives, `REGISTER` intact, and no silent Asterisk fallback; and no durable media table, RuntimeNode, registry capability, tenant, Redis, outbox, web-admin, or Artisan authority changed. T3 is not complete. T3 must also introduce the minimum Kamailio application-dialog route for registered browser conference media, with new-dialog routing intrinsically excluding ineligible execution RuntimeNodes while keeping registration eligibility separate. `docs/decisions/ADR-020-t3-rtp-media-plane.md` fixes the RTP media-plane architecture, `docs/evidence/t3/t3-rtp-media-preparation-audit.md` records the criterion matrix and first-slice authority, and `docs/evidence/t3/t3-s1-rtpengine-foundation-implementation.md` records the implemented pinned relay foundation: rtpengine as shared platform infrastructure in `utcp-platform` (single-replica Deployment, no RuntimeNode or registry capability, mirroring ADR-019's Kamailio precedent); in-cluster-only networking with ng control on ClusterIP 2223/UDP and media on 40000-40099/UDP advertised on the Pod IP; userspace forwarding (`--table=-1`) so PSA `restricted:v1.35` holds with no exception; a digest-pinned repository-built image following the Asterisk precedent; one new `allow-rtpengine-media` NetworkPolicy with default-deny preserved; and no new durable tables. RTP never traverses the HTTP/WSS 443 route, so gateway and k3d guards are retained. T3-S1 adds bounded guard replacements and no live SIP media routing; browser-reachable media, Kamailio INVITE route authority, and conference admission remain deferred to later T3 slices and V0. | **T3-S1 is Complete.** The final scrape-discovery proof at `bde02b7` applied exactly one resource — `NetworkPolicy/utcp-observability/allow-prometheus-operator-apiserver-egress`, selecting `app.kubernetes.io/component: prometheus-operator` with egress limited to the pinned API endpoint `172.24.0.2/32` on TCP `6443` — and proved all four remaining outcomes: the Prometheus Operator reached the Kubernetes API (`connection established kubernetes_version=1.35.3+k3s1`, API capabilities discovered, informers synced, zero errors in the entire post-recovery log, and no TLS, authorization, or service-account error replacing the policy error); it recovered to Ready through its **own** `5m0s` crash-loop backoff with no manual rollout (restart `396`->`397`, same Pod UID, Deployment `1/1/1`, then stable for 9 minutes with the restart count frozen at 397); the unchanged rtpengine `PodMonitor` (generation `1`, rv `423578`) was reconciled by normal Operator authority, moving the generated configuration secret from rv `260855` to `426909` and `podMonitor` jobs from `0` to `1`, producing `podMonitor/utcp-observability/rtpengine/0` whose namespace selector, both pod-label selectors, `/metrics` path, and named `metrics` port resolving to TCP `2224` all match the PodMonitor exactly; and the rtpengine target is discovered and `up` at `http://10.42.1.216:2224/metrics` with an empty `lastError`, a recent `lastScrape`, and a `2.3ms` scrape duration, with `rtpengine_ports = 100`, `rtpengine_ports_free = 100`, `rtpengine_ports_used = 0`, and `rtpengine_sessions = 0` ingested and queried **through Prometheus itself** and labelled `namespace=utcp-platform`, `pod=rtpengine-74cd786966-hvcrn`, `container=rtpengine`, `instance=10.42.1.216:2224`. Default-deny remained intact, no public control, metrics, or media exposure was introduced, rtpengine, Prometheus, and Kamailio all retained identical Pod UIDs, restart counts, and images with no rollout, and no canonical state or Kamailio runtime configuration changed. All four defects in the T3-S1 arc (`PRODUCT_DEFECT-1` through `PRODUCT_DEFECT-4`) are now closed. T3 remains In Progress: the next bounded slice per ADR-020 sections 5 and 7 is the Kamailio application-dialog media route (SDP `offer`/`answer`/`delete` with `REGISTER` untouched), consuming the ClusterIP control endpoint this proof established.
| V0 Natural login, SIP registration, and conference admission | Planned     | The target vertical slice is natural login -> SIP REGISTER over WSS -> conference admission, including proof that the T3 internal application-dialog route uses canonical Conference RuntimeNode eligibility and has automatic cutoff/restoration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| T4 FreeSWITCH ESL parity                                     | Planned     | No FreeSWITCH integration exists yet.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| T5 Convergence, failover, and recovery                       | Complete    | T5 evidence now proves the multi-RuntimeNode Asterisk topology, deterministic failover and restoration recovery, listener ownership/liveness/degradation/automatic recovery, symmetric degraded/recovered events, deterministic capacity-aware placement, pending-no-capacity and automatic retry, recovery metric-event retention with scheduled pruning, and this repository Namespace PSA authority reconciliation. The controlled live Namespace PSA proof is complete: `f959f00` recorded the T5-A78 corridor against `utcp-local` (declarative apply of the canonical `pod-security-labels.yaml`, all five UTCP namespaces at `restricted`/`v1.35` including the previously missing `utcp-runtime` version pins, compliant-Pod admission, `restricted:v1.35`-attributed privileged-Pod rejection, migration-Job admissibility, drift introduction and declarative correction, idempotent reapply, unchanged NetworkPolicy count, and full workload health), satisfying every point of the deferred live acceptance contract. The final phase-closure evidence is now recorded in `docs/evidence/t5/t5-phase-closure.md` (`T5_COMPLETE`), which reconciles all 21 canonical T5 criteria to `SATISFIED` against the existing corpus without rerunning any proof, confirms no later change invalidates that evidence (`make security-config-check` re-validated at HEAD with `namespace_psa_authority=ok` and `restricted_workload_compatibility=ok`), and documents why the corridor corpus stays at its historical `docs/evidence/t2/multi-node-failover-readiness.md` path. No T5 implementation, live runtime proof, or documentation work remains. Hosted CI is an R0 release requirement, not a T5 exit criterion. See also `docs/evidence/roadmap/t1-t5-roadmap-reconciliation.md`. The former Kamailio signaling-cutoff item is not an active T5 gap because current Kamailio is registration-only and has no runtime dialog route. Hosted GitHub Actions execution has not been observed for the uncommitted working tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| C6/C7/T6/V1 call routing and external trunks                 | Planned     | Future canonical call intent, route/trunk decision, eligible runtime selection, projected Kamailio signaling execution, and external-trunk/general-call cutoff belong to the extended call-routing phases, not current T5.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| UI-A Application Shell, Routing, and Navigation | Complete | UI-A1 added Vue Router, route-level views, shared `AppShell`, `/dashboard`, capability-aware navigation, and forbidden/not-found states; UI-A2 controlled natural Playwright browser proof (evidence `docs/evidence/ui/ui-a2-application-shell-dashboard-browser-proof.md`, deployed web digest `sha256:29abd658…`) proved login/forced-change, dashboard landing with canonical API-backed summaries, capability-aware navigation for broad and zero-capability users, direct URL/reload, browser back/forward, forbidden/not-found, tenant switching, transient secret handling, and logout with post-logout protection. |
| UI-B Design System and Reusable Component Library | Complete | UI-B1 added semantic tokens, system/light/dark themes, local appearance preference storage, core accessible primitives, and adoption by `AppShell`, Dashboard, and `/admin/users`; UI-B2 controlled browser proof (evidence `docs/evidence/ui/ui-b2-themes-components-browser-proof.md`, deployed web digest `sha256:f3e23437…`) proved the theme contract, media response, explicit-preference persistence, invalid-value recovery, appearance-only storage, accessible no-API theme control, both-theme adoption, component semantics, responsive/reduced-motion, AA core-token contrast, and no wrong-theme flash; UI-B2 remaining-view repository implementation (evidence `docs/evidence/ui/ui-b2-remaining-view-component-adoption.md`) migrated Login, Change Password, Tenants, Memberships, Runtime Nodes, and User Detail to shared components and corrected the Users mobile metadata overflow contract; UI-B3 controlled natural browser proof (evidence `docs/evidence/ui/ui-b3-management-view-browser-proof.md`, deployed web digest `sha256:4ab3349e…`) proved natural login, forced change-password, Tenants, Memberships with server `admin/roles` catalog authority, Runtime Nodes with write-only credential secrecy, Users, User Detail, light/dark with no theme API requests, keyboard/component semantics, and logout with post-logout protection, but found the Users 375px page-level overflow is not corrected (`documentElement.scrollWidth 691` vs `innerWidth 375`; the mobile `.data-row { flex-direction: column }` rule leaves the base `flex-wrap: wrap`, wrapping row metadata into a second off-viewport column); UI-B4 repository correction (evidence `docs/evidence/ui/ui-b4-users-mobile-overflow-fix.md`) resets narrow `.data-row` wrapping and height through `apps/web/src/style.css` and adds a hygiene regression contract; UI-B5 focused browser proof (evidence `docs/evidence/ui/ui-b5-users-mobile-overflow-live-proof.md`, deployed web digest `sha256:fd9d2201…`) live-proved the `53f0f32` frontend and confirmed `/admin/users` has no page-level overflow at 375px in Light (`scrollWidth 375` == `innerWidth 375`) or Dark (`375` == `375`) with the metadata subgrid stacked in one in-row vertical column, zero out-of-bounds elements and no overflow clipping, theme switching making no API request or data change, and the desktop 1280px layout preserved — closing UI-B. |
| UI-C Data Interaction and Management Workflows               | Complete    | Pagination, forms, typed `ApiRequestError`, and management pages exist; UI-C1 repository implementation adds shared resource/action state contracts, one app-level notification authority, correct informational login bootstrap `401` handling, on-demand RuntimeNode details with in-memory cache invalidation, node-scoped RuntimeNode credential/form IDs, and representative RuntimeNode/User/Membership mutation feedback (evidence `docs/evidence/ui/ui-c1-async-state-notifications-runtime-node-loading.md`). UI-C2 controlled browser proof (evidence `docs/evidence/ui/ui-c2-async-state-notifications-runtime-node-browser-proof.md`, deployed web digest `sha256:3fd8ac58…`) live-proved informational pre-login `401` versus submitted-failure error, 110 RuntimeNodes loading with 2 shared requests and zero per-node detail fan-out, on-demand single-node details with bounded in-memory reuse, tenant-switch detail invalidation with canonical reread, success and controlled-failure notifications, unique notification IDs, unique node-scoped RuntimeNode credential IDs, and single-node detail-failure isolation with successful retry. UI-C3 repository implementation (evidence `docs/evidence/ui/ui-c3-list-query-filter-sort-pagination.md`) adds a shared typed URL-backed list-query contract, shared filter/pagination/list/summary primitives, deterministic invalid-query normalization, Vue Router history semantics, Users search/status/page/page-size restoration, unsupported-parameter cleanup for Memberships/Tenants/Runtime Nodes, and preservation of the RuntimeNode no-detail-fan-out request budget without fake client-side sorting. UI-C4 controlled browser proof (evidence `docs/evidence/ui/ui-c4-list-query-history-pagination-browser-proof.md`, deployed web digest `sha256:47232dd1…`) live-proved on `dd03c21` the Users direct-URL/reload restoration, filter and page-size changes resetting page to 1, page-only navigation, unchanged-Apply no-op, Back/Forward restoration, invalid-query normalization via replacement without loops, sorting-control absence, Memberships/Tenants unsupported-parameter stripping with server role catalog and unchanged tenant context, the preserved RuntimeNode 2-request/zero-fan-out budget with scoped on-demand details, tenant-switch list/detail isolation, refresh data retention with failure distinct from empty, 375px responsive integrity, and a storage boundary of only `utcp.appearance`; but found a blocking stale-response defect (a delayed older Users query overwrites the newer rendered list because `refreshUsers` writes module `users`/`userPagination` outside the `useAsyncResource` `requestId` guard the view does not render from). UI-C5 repository implementation (evidence `docs/evidence/ui/ui-c5-users-stale-response-fix.md`) corrects that defect by rendering Users rows and pagination from the guarded current-request result, applying accepted results to legacy module refs only after the request-generation guard accepts them, and invalidating the Users resource generation on tenant switch. UI-C6 controlled browser proof (evidence `docs/evidence/ui/ui-c6-users-stale-response-live-proof.md`, deployed web digest `sha256:7cc0e769…`) live-proved the correction on `a021fd0` and closed the blocking stale-response gap: a held obsolete query A (206 active, 11 pages, Next enabled) released after query B (1 suspended, 1 page, Next disabled) rendered did not overwrite B's rows or pagination, tenant-switch isolation held via the same request-generation guard, and normal refresh plus a disposable-user mutation reread the guarded current query without resetting filter/page intent. UI-C8 repository implementation (evidence `docs/evidence/ui/ui-c8-runtime-adapter-configuration-descriptor-contract.md`) adds the canonical server adapter-configuration descriptor contract: descriptors are published through adapter-owned configuration handlers, Asterisk ARI exposes all seven current configuration fields with backend-owned defaults and validation hints, simulator-deterministic exposes its current profile fields because it is configuration-capable, unavailable adapters expose no writable descriptor collection, and malformed descriptors fail deterministically without exposing secrets. UI-C9 repository implementation (evidence `docs/evidence/ui/ui-c9-catalog-runtime-forms-management-actions.md`) consumes those descriptors in Runtime Nodes: text/integer/json fields render generically, the handwritten Asterisk form and payload helpers are removed, simulator JSON configuration uses the same renderer, unsupported required descriptors block submission, payloads preserve canonical descriptor keys and write-only behavior, lazy RuntimeNode details remain bounded, and current Tenants, Memberships, Users, User Detail, and Runtime Nodes mutations use shared keyed action state with safe notifications and canonical rereads. UI-C10 controlled browser proof (evidence `docs/evidence/ui/ui-c10-catalog-runtime-forms-actions-browser-proof.md`, deployed web `sha256:7ca56c7e…`, API `sha256:7d6a24f4…` from `6e31763` incl. the `4eaea84` descriptor publisher) live-proved zero per-node fan-out over 110 nodes, Asterisk rendering exactly its 7 descriptor fields in canonical order with catalog constraints and node-scoped IDs and no handwritten form, simulator JSON via the generic renderer, invalid JSON blocking with zero requests, canonical simulator save with parsed-JSON payload and reread, unsupported required descriptor blocking, blank write-only omission with no value leak, action-scoped User/Tenant/config mutations with success notifications and canonical rereads, a controlled failed action that stayed retryable, authoritative backend validation (message-level errors surfaced via the shared notification), tenant-switch clearing of forms/errors/detail caches, and 375px + theme integrity — closing UI-C. |
| UI-D Real-Time Telephony Operational Experience              | Complete    | Static session and signaling panels exist; remaining gap is natural proof of the new RuntimeNode Reverb/WSS corridor plus broader runtime health, Conference, participant, runtime-operation, and audit views, partly dependent on T2/T3/V0 and later call-control phases. UI-D0 contract audit (evidence `docs/evidence/ui/ui-d0-realtime-operational-contract-audit.md`) selected UI-D1 as the first notification-only RuntimeNode slice. UI-D1 repository implementation (evidence `docs/evidence/ui/ui-d1-runtime-node-realtime-implementation.md`) installs Laravel Reverb plus Echo/Pusher, adds one queued after-commit `RuntimeNodeOperationalStateChanged` notification envelope, bridges canonical `runtime_node.*` outbox messages, authorizes `tenant.{tenantId}.runtime-nodes` through `/api/broadcasting/auth` with the existing session and `runtime.nodes.view`, deploys Reverb with generated local credentials, WSS `/app/` routing, and bounded NetworkPolicy, and wires one frontend realtime lifecycle authority so notifications/reconnects reread canonical RuntimeNode snapshots while disconnection preserves stale-but-usable data. UI-D2 live-proof attempt (evidence `docs/evidence/ui/ui-d2-runtime-node-realtime-browser-proof.md`) was blocked before browser testing by the `utcp-migrate` Job using `BROADCAST_CONNECTION=reverb` without Reverb credentials. UI-D3 repository correction (evidence `docs/evidence/ui/ui-d3-migration-broadcast-driver-fix.md`) restores the migration overlay to `BROADCAST_CONNECTION=log`, removes migration Reverb transport properties, adds rendered-manifest/static coverage proving the migration Job holds no Reverb Secret or application credentials, and preserves Reverb broadcasting plus credentials for API, worker, outbox-dispatcher, and Reverb. UI-D remains In Progress pending focused natural Playwright MCP proof of WSS connection, private-channel authorization, canonical event-triggered rereads, reconnect behavior, tenant isolation, logout disconnect, zero detail fan-out, and no secret/protected payload exposure. UI-D4 resumed live proof (evidence `docs/evidence/ui/ui-d4-runtime-node-realtime-live-proof.md`, built from `18ef90a`) **confirms the UI-D3 correction**: `make k8s-apply` succeeds and `utcp-migrate` completes with `BROADCAST_CONNECTION=log` and no Reverb credentials. It also **proves the public transport corridor** — Reverb ClusterIP-only on `8080` with zero NodePort/LoadBalancer/hostPort in the rendered overlay, canonical edge routes 200, `/app/` reaching Reverb instead of the SPA catch-all, `/api/broadcasting/auth` on the API, and a real `101 Switching Protocols` from Laravel Reverb through `wss://app.utcp.local.test` (Traefik 443) — plus natural login, tenant selection, the 2-request/zero-per-node-detail RuntimeNode budget over 110 nodes with subscription gated behind the canonical snapshot, and the `utcp.appearance`-only storage boundary. **Two bounded configuration defects still block the behavioural proof:** `REVERB_ALLOWED_ORIGIN` carries a scheme while Reverb matches the parsed Origin host, so every client is rejected with `4009 Origin not allowed` (the browser opened exactly one correct `wss://…/app/<key>` socket, closed 1006, zero channel-auth requests); and `security/data/allow-redis.yaml` omits the `reverb` role, so the scaling `RedisPubSubProvider` cannot reach Redis. UI-D5 repository correction (evidence `docs/evidence/ui/ui-d5-reverb-origin-redis-policy-fix.md`) changes local Reverb allowed origins to host-only `app.utcp.local.test`, derives the fallback origin hostname from `APP_URL`, and permits Redis ingress from role `reverb` only on TCP `6379` while preserving migration on `log` and publisher workloads on Reverb. UI-D1 remains not live-proven. UI-D6 resumed live proof (evidence `docs/evidence/ui/ui-d6-runtime-node-realtime-live-proof.md`, built from `ff3444f`) **live-proves both UI-D5 corrections and the whole server-side corridor**: the browser's exact Origin now yields `101 Switching Protocols` + `pusher:connection_established` (UI-D4 got `4009 Origin not allowed`), `reverb → redis:6379` is OPEN with 0 restarts, migration stays credential-free on `log`, publishers report `reverb` + host-only origin, Reverb remains ClusterIP-only on `8080`, edge routes and `/app/` and `/api/broadcasting/auth` all route correctly, and natural login plus the 2-request zero-per-node-detail budget over 110 nodes hold with an `utcp.appearance`-only storage boundary. **A third, frontend blocking defect remains:** the browser never opens a WebSocket (zero WS events, zero `/app/` and `broadcasting/auth` requests, no JS errors) although the badge class proves the client reached `new Echo(...)` and `.private(...)`, isolating the fault to `createEchoClient`'s pusher-js options (`enabledTransports: ['wss']`, and latently `wsPath: '/app'`); no test covers it because the suite stubs the Echo factory. A 375 px page-level overflow regression on `/admin/runtime-nodes` (406 vs 375) was also found. UI-D7 repository correction (evidence `docs/evidence/ui/ui-d7-browser-reverb-client-mobile-fix.md`) fixes those frontend blockers by testing and using a production Echo option builder with `enabledTransports: ['ws', 'wss']`, `forceTLS`, no `wsPath`, no polling/fallback public port/secret-bearing option, and by making the Runtime Nodes section heading and live-update badge wrap/stack at the established narrow breakpoint without root overflow clipping. UI-D1 remains not live-proven. UI-D8 controlled natural browser proof (evidence `docs/evidence/ui/ui-d8-runtime-node-live-behavior-proof.md`, web `sha256:e9297a6f…` from `d49afb7`) **live-proves the UI-D1 corridor end to end**: the application browser opened exactly one socket to `wss://app.utcp.local.test:443/app/<key>` with a single `/app/` segment, reached `connection_established` → `subscribe` → `subscription_succeeded` and showed "Live updates connected"; `POST /api/broadcasting/auth` returned 200 on the real session for the tenant channel; a Web Admin descriptor-form `seed` change produced a broadcast carrying exactly the five approved fields and triggered **1** list reread plus **3** scoped detail requests for the open node and **0** for the other 110 nodes, with the restore following the same path; Reverb scale-to-zero preserved data with a stale badge and zero requests, and recovery reconnected with the stale badge clearing at +509 ms **after** the canonical reread completed at +269 ms; tenant switch unsubscribed the old channel with zero fan-out and a second context's Local Tenant mutation produced zero accepted events in the primary; logout closed the socket with code 1000 and zero post-logout auth; and `/admin/runtime-nodes` measured zero overflow at 375 px in Light and Dark, closing the UI-D6 regression. Three bounded non-blocking follow-ups were recorded (duplicate initial catalog+list round, false "stale" badge after tenant switch, vendored `pusherTransportTLS` storage entry). UI-D9 repository implementation (evidence `docs/evidence/ui/ui-d9-conference-participant-live-implementation.md`) extends the proven corridor to Conferences and participants with one notification-only Conference broadcast for `conference.*` and `conference_participant.*`, the tenant channel `tenant.{tenantId}.conferences` authorized by session active tenant plus `telephony.conferences.view`, one shared Echo authority serving RuntimeNode and Conference channels, a capability-gated `/operations/conferences` read-only route, canonical list/detail/participant rereads, selected-Conference scoped notification/reconnect reloads, and repository coverage for the three UI-D8 follow-ups: RuntimeNode initial catalog/list is 1+1 with zero detail fan-out, tenant re-subscription clears stale after snapshot plus subscription, and `pusherTransportTLS` is bounded non-sensitive vendor metadata. UI-D stays In Progress pending the Conference/participant natural browser proof plus remaining runtime-operation and audit surfaces. UI-D10 controlled natural browser proof (evidence `docs/evidence/ui/ui-d10-conference-participant-live-proof.md`, api `sha256:7402da65…` / web `sha256:ecab7ce1…` from `39627da`) **proves the Conference/participant live corridor but is blocked from completion**: capability-gated `/operations/conferences`, a 1-request Conference list budget with zero detail/participant fan-out over 120 Conferences, natural channel auth 200 for `private-tenant.{tenantId}.conferences` on one shared WebSocket client, scoped selection, real `conference.created`/`desired_state_changed` and `conference_participant.admitted`/`removed` deliveries whose envelopes carry exactly the six approved fields with no participant, endpoint, credential, signaling or media data, 1 list + 1 selected-detail + 1 selected-participant reread per notification with zero unrelated resources, unrelated-Conference events causing only a list reread, correct stale presentation under Reverb outage, clean logout teardown, a bounded `pusherTransportTLS` vendor cache, and zero 375 px overflow; the RuntimeNode duplicate-request follow-up is confirmed fixed (1 catalog + 1 list + 0 detail). **Blocking regression:** readiness is signalled through `channel.bind?.('pusher:subscription_succeeded', …)` but Laravel Echo channels expose `subscribed(callback)` and have no `bind`, so the call no-ops, the badge never reaches connected, and no canonical resynchronization runs after a reconnect (socket healthy 105 s with zero rereads, view silently stale). Tenant-switch isolation and previous-tenant rejection were therefore not proven. UI-D12 controlled natural browser proof (evidence `docs/evidence/ui/ui-d12-echo-readiness-live-proof.md`, web `sha256:f1395fb3…` from `0b46434`) **live-proves the UI-D11 correction and closes the UI-D10 blocker**: the served bundle uses Echo `subscribed()`/`error()` with generation fencing and no channel-level `bind`; RuntimeNode (1 catalog + 1 list + 0 detail) and Conference (1 list + 0 detail + 0 participants) both reach "Live updates connected" on one shared socket; reconnect performs exactly one bounded resync per generation (Conference 1 list + 1 detail + 1 participants, stale cleared at +353 ms after the rereads; RuntimeNode 1 list + 3 scoped open-node rereads + 0 unrelated, stale cleared at +439 ms), with socket-open and first-subscription alone never clearing stale and the previous 105-second stuck-"reconnecting" state absent; tenant switching leaves the old channel and reaches connected with zero socket recreation and no false stale badge; a rapid Local→Proof→Local sequence leaves only the final tenant's channel; previous-tenant events cause zero accepted events and zero rereads; logout during reconnect prevents revival (45 s, zero sockets/frames/auth/rereads) and normal logout closes the single socket with code 1000; storage stays bounded to `utcp.appearance` plus a metadata-only `pusherTransportTLS`. UI-D then delivered and live-proved the remaining T1-achievable operational surfaces: Runtime Operations (UI-D13/D14 implementation, UI-D16 PostgreSQL UUID + producer-backed outbox correction, UI-D17 corrected end-to-end proof), Runtime Reconciliations (UI-D18/D19 implementation, UI-D21 no-op event-storm suppression, UI-D22 quiescence proof), and Audit (UI-D23 read API, UI-D24 read-only view, UI-D25 natural live proof at `docs/evidence/ui/ui-d25-audit-live-proof.md`, api `sha256:9a33ef67…` / web `sha256:a2f4777c…` from `ae5cd5f`). UI-D25 proved the `/admin/audit-records` corridor: capability-gated read-only route with no write/export control, 1 list + 0 detail requests on entry over 2834 records with 0 Audit WebSockets, server-backed pagination, all nine canonical filters, a 422 validation surface, on-demand single-detail loading, a 25-record cross-action safe-contract scan with zero sensitive fields, a 125 s idle window with zero automatic refresh, a canonical Web Admin desired-state change appending one real `runtime_node.desired_state_changed` audit row via `AuditRepository::append()` that appeared only through explicit Refresh, bounded storage, and zero 375 px overflow. Audit realtime is intentionally absent (no canonical Audit event/channel). UI-D is Complete for its T1-achievable scope; the remaining Included-Scope items — live TelephonySession state and runtime/listener-health views — are dependency-gated on T2/T3/V0 and carried by those phases. `UTCP_PHASE=T1` unchanged. |
| UI-E Accessibility, Testing, Responsiveness, and Portfolio Quality | Complete | ARIA labels, focused frontend tests, T1 natural browser proof, and responsive contracts exist; UI-B2 adds migrated-view accessibility assertions and a Users narrow-layout overflow regression contract. UI-E1 repository enforcement (evidence `docs/evidence/ui/ui-e1-accessibility-static-unit-enforcement.md`) adds Vue accessibility linting through the canonical ESLint flat config, an axe-core Vitest helper that fails serious/critical violations, shared primitive coverage, primary UI-A through UI-D route-view axe assertions, documented accessibility/keyboard/focus/responsive/test/browser-proof conventions, and removes stale hard-coded frontend test-count wording in favor of the canonical test command. UI-E2 natural accessibility browser proof (evidence `docs/evidence/ui/ui-e2-accessibility-browser-proof.md`, web `sha256:81256c83…` from `4e2dfcb`) live-proved most of the baseline — real-browser axe with 0 serious/0 critical (contrast included) on all ten primary routes in Light and Dark, keyboard-operable login/AppShell/data views, validated form-label associations and both lint suppressions, non-color status, zero root overflow at 375/768, painting focus rings, and clean console/network/storage/logout hygiene — but returned INCOMPLETE on one focus-management defect: a focused `UiButton` is natively disabled while `loading` (`:disabled="disabled || loading"`), so keyboard focus is stranded on `<body>` on every loading action (Refresh, detail open, filter Apply, pagination), and inline-detail close drops focus to `<body>` instead of the trigger. One non-blocking moderate `page-has-heading-one` finding remains on the Login/Change-password pages. UI-E3 repository correction (evidence `docs/evidence/ui/ui-e3-focus-management-correction.md`) removes loading-driven native disabling from `UiButton`, keeps explicit disabled native, exposes loading through `aria-disabled`/`aria-busy`, blocks loading click/Enter/Space/submit duplicate activation at the shared component boundary, and restores Audit detail close focus to the exact opener without rereads. UI-E4 focused natural browser live proof (evidence `docs/evidence/ui/ui-e4-focus-management-live-proof.md`, web `sha256:7f2550c5…` from `4d00dec`, all other workloads preserved) live-proved both UI-E2 focus defects corrected: Audit Refresh and per-row Details retain `document.activeElement` throughout loading (never `<body>`), stay natively enabled with `aria-disabled="true"`/`aria-busy="true"` that clear on completion, keep their accessible names and a painting focus ring while busy; repeated `Enter`/`Space`/pointer activation caused 0 additional requests on every `loading`-bound control, including a live Tenants Create-tenant submit that also rejected `Enter` from a form field (1 POST); explicit disabled controls stayed natively disabled, unfocusable, and request-free; Audit detail close returned focus to the exact opener with 0 rereads; selection switch retargeted restoration to record B; detached-opener handling raised no error. Bounded axe returned 0 serious/0 critical (0 total) on list, detail-open, detail-closed, and busy states in Light and Dark, with clean console, network, storage, and keyboard-logout hygiene. UI-E4 is INCOMPLETE on one separate pre-existing defect (`PRODUCT_DEFECT-3`): `UiPagination` never passes `:loading` to its `UiButton`s, so repeated activation during a pending page load issues additional page requests; pagination focus retention itself passed. UI-E5 repository correction (evidence `docs/evidence/ui/ui-e5-pagination-loading-correction.md`) gives `UiPagination` an explicit default-false `loading` prop, forwards it to Previous and Next, and wires every production caller to the existing canonical visible-list pending state (`isRefreshing`), with component and representative Audit Records plus Runtime Operations tests proving available pending controls remain focusable with loading ARIA, repeated click/Enter/Space emits zero additional page events or requests, and boundary controls remain natively disabled. UI-E6 focused natural browser live proof (evidence `docs/evidence/ui/ui-e6-pagination-loading-live-proof.md`, web `sha256:54403c5d…` from `c40a16b`, all other workloads preserved) live-proved the correction on Audit Records (2843 records/143 pages), Runtime Operations (`status=succeeded`, 4675/234 pages), and Users (207/11 pages): an available pagination control with a pending list request retains focus, stays natively enabled, exposes `aria-disabled="true"`/`aria-busy="true"` that clear on completion, preserves its accessible name and `button` role, and keeps a visible focus ring in Light and Dark while busy; repeated Enter/Space/pointer activation produced 0 additional requests and 0 additional page-change events (counted independently of HTTP via `history` instrumentation, proving guard suppression rather than URL dedupe — the UI-E4 `page=2, page=2, page=3, page=4` sequence is gone); one activation always produced one request; page-one Previous and final-page Next stayed natively disabled with no loading ARIA, refused focus, and produced 0 requests; filters, page size, and ordering survived every page change with 0 detail fan-out; axe returned 0 serious/0 critical while loading and after completion on both routes; console, network, storage, and keyboard-logout hygiene were clean. No product defect remains. UI-E7 repository implementation (evidence `docs/evidence/ui/ui-e7-responsive-contract-enforcement.md`) codifies the supported 375px, 768px, and desktop responsive contract, route inventory, shared sizing/wrapping/filter/form/long-identifier/list/pagination/detail-panel hooks, deterministic structural tests, and repository-hygiene enforcement against root overflow clipping and unsafe missing responsive hooks. No browser proof was run for UI-E7. UI-E8 focused natural responsive browser live proof (evidence `docs/evidence/ui/ui-e8-responsive-contract-live-proof.md`, web `sha256:64e616bd…` from `b53fb9e`, all other workloads preserved) live-proved the whole responsive contract: all ten primary routes satisfy `documentElement.scrollWidth <= innerWidth` at 375x812 (Light and Dark), 768x1024, and 1440x900 with 0 elements crossing the viewport edge, and `overflow-x` computes `visible` on root and body everywhere with no `hidden`/`clip` on `#app`, `.app-shell`, `.shell`, `.workspace`, `.shell-content`, `main`, `body`, or `html`, so no pass is produced by concealed overflow; AppShell narrow navigation, heading/action wrapping (0 overlaps), `UiFilterBar` collapse (`225px 225px` to `317px`), 259 long identifiers contained and unclipped across four routes, pagination contained and outside any clipped or scrolling ancestor, zero local horizontal-scroll regions, detail grids collapsing without widening the document, text-bearing badges with 0 non-nested overlaps, empty/loading/validation layouts holding width, 34/375px and 29/768px tabbed controls all `:focus-visible` at a uniform 3px with 0 obscured and 0 clipped, 0 requests on resize/theme/tabbing with exactly one per explicit action, and an `utcp.appearance` + `pusherTransportTLS`-only storage boundary. UI-E8 is INCOMPLETE on one separate pre-existing non-responsive defect (`PRODUCT_DEFECT-4`): the shared `button:hover:not(:disabled), .ui-button:hover:not(:disabled)` rule (`apps/web/src/style.css:204-207`, from `3b643d0`) sets a hover `background` without a matching `color`, so `.ui-button--secondary` (1.55:1 dark / 1.65:1 light) and `.ui-button--ghost` (1.23:1 / 1.38:1) fail contrast while hovered; axe returned 1 serious violation in hovered states and 0 serious / 0 critical in every resting and keyboard-reachable state. No responsive-layout defect was found. UI-E9 repository correction (evidence `docs/evidence/ui/ui-e9-button-hover-contrast-correction.md`) removes the conflicting shared `.ui-button:hover` background authority and makes hover foreground/background pairs explicit per variant using existing tokens; deterministic coverage calculates Light primary 10.57:1, secondary 15.45:1, ghost 7.63:1, danger 6.57:1 and Dark primary 8.93:1, secondary 10.23:1, ghost 7.29:1, danger 9.03:1, while preserving loading `aria-disabled`/`aria-busy`, native disabled behavior, focus-visible, and responsive selectors. UI-E10 focused natural hover-contrast browser live proof (evidence `docs/evidence/ui/ui-e10-button-hover-contrast-live-proof.md`, web `sha256:dce1a124…` from `7eae240`, all other workloads preserved) live-proved the UI-E9 correction and closed `PRODUCT_DEFECT-4`: on real pointer hover over existing non-destructive controls (primary `Create runtime node`/`Log out`, secondary `Refresh`, ghost Audit inline-detail `Close`, danger `Disable` hovered but never activated), browser-measured contrast matched UI-E9's calculated ratios exactly — Light primary 10.57:1, secondary 15.45:1, ghost 7.63:1, danger 6.57:1; Dark primary 8.93:1, secondary 10.23:1, ghost 7.29:1, danger 9.03:1 — with transparent computed backgrounds resolved against the first opaque painted ancestor rather than transparent black. Real-browser axe (`color-contrast` enabled, no rule group excluded) returned 0 serious/0 critical (0 total) in all eleven tested states including hovered secondary and ghost in Light and Dark, replacing UI-E8's 1 serious violation. Hover and `:focus-visible` coexist under real keyboard Tab with the 3px ring painted and unobscured and `activeElement` retained; a loading `Refresh` holding one intercepted request keeps `native disabled=false`/`aria-disabled=true`/`aria-busy=true`, receives no active hover treatment, stays readable at 17.74:1, and absorbs repeated Enter/Space/pointer activation with 0 additional requests; the naturally disabled page-1 Previous receives no active hover treatment, refuses focus, and issues 0 requests; and hover changed no button dimension at 375x812 (Light and Dark) or desktop with root `scrollWidth <= innerWidth` held while hovered, so no responsive regression exists. Hygiene held: 0 page errors, 0 console warnings, one session-wide pre-login `401 /auth/session` probe, 0 requests on hover/theme-change/tabbing with exactly one per legitimate action, and an `utcp.appearance` + `pusherTransportTLS`-only storage boundary. Disabled-control contrast of 3.90:1 under the established `--color-disabled` contract is recorded as EXPECTED_BEHAVIOR (WCAG-exempt inactive component, unflagged by axe). No product defect remains and the responsive live-proof corridor is Complete. UI-E11 portfolio-finish preparation audit (evidence `docs/evidence/ui/ui-e11-portfolio-finish-preparation-audit.md`, read-only against the running `7eae240` web image) establishes that only the roadmap criterion "The UI presents UTCP as one coherent portfolio-quality product" remains open, and defines the exact bounded scope of the final slice. Through natural login, `Local Tenant` selection, and a read-only sweep of all nine authenticated routes at desktop and 375x812 it records four MUST_FIX findings — no product orientation anywhere (the product name appears exactly once in the whole application and Login states nothing about what UTCP is); eight of nine routes render no page description; user-visible terminology uses four casing conventions for the same construct (`27 tenants` / `Page 1 · 2849 Audit records` / `Page 1 · 4730 Runtime Operations` / `110 RuntimeNodes`) with three inside `RuntimeNodesView.vue` alone and PascalCase API type names leaking into copy that contradicts the sentence-case navigation labels; and the Dashboard `Refresh` rendering `variant="primary"` with no `:loading` binding (measured live: `aria-busy` and `aria-disabled` both null while reloading) against `variant="secondary"` plus `:loading` on all eight other routes. Three SHOULD_FIX findings are recorded (ungrouped nine-entry navigation whose ordering is already correct and whose grouping must suppress capability-empty headings; the still-open UI-E2 `page-has-heading-one` finding on Login/Change-password; and `UiEmptyState` offering no next-step action, mitigated by an existing Clear control). Heading hierarchy, active navigation state, mobile menu hierarchy, destructive-action placement, navigation ordering, topbar placement, and spacing/density/badge/surface consistency are confirmed not defects. One bounded slice (UI-E12) is recommended across ten view templates with no change to `style.css`, tokens, router, capabilities, state, or component contracts, and deterministic UI-E closure criteria are specified. Remaining gap: focused natural browser UI-E closure proof, plus one deferred non-blocking `UiFilterBar` Apply busy-state limitation. UI-E12 repository implementation (evidence `docs/evidence/ui/ui-e12-portfolio-finish-implementation.md`) completes the bounded portfolio presentation slice in the repository: Login and forced password change now have single contextual H1s, every authenticated primary route keeps `class="section-heading"` with concise purpose copy, visible domain terminology and count summaries use sentence case, Dashboard Refresh is secondary and bound to the canonical loading state, and AppShell navigation is grouped conceptually with capability-empty headings suppressed; focused frontend tests cover those contracts plus axe assertions, loading focus, duplicate activation, mobile hierarchy, and capability filtering. UI-E13 final portfolio presentation live proof (evidence `docs/evidence/ui/ui-e13-portfolio-finish-live-proof.md`, web `sha256:7ff1f594…` from `3ba2e73`, all other workloads preserved) live-proves that slice and closes UI-E: Login renders exactly one `<h1>` (`Unified Telephony Control Plane`) with purpose copy covering tenant access, runtime nodes, lifecycle operations, reconciliation, and audit evidence at 1440x900 and 375x812 in Light and Dark with the Sign in task and associated labels intact; forced password change renders one task-oriented `<h1>` (`Secure your UTCP account`) with UTCP context and natural keyboard order; all nine authenticated routes carry accurate purpose copy with Refresh as secondary and `H2 → H3` preserved; rendered-text scans across five routes with detail panels open returned zero `RuntimeNodes`, `TelephonySessions`, and Title Case domain-form matches, and all eight count summaries are sentence case (`110 runtime nodes`, `122 conference operations`, `Page 1 · 4730 runtime operations`, `Page 1 · 2851 audit records`); Dashboard shows zero primary-variant content buttons and a bounded hold proved the canonical three-request summary budget with `document.activeElement` retained on Refresh, `native disabled=false`/`aria-disabled=true`/`aria-busy=true`, a painting 3 px ring while busy, and zero additional sequences from repeated Enter/Space/pointer activation; navigation renders four groups (`Overview`, `Access and tenancy`, `Evidence`, `Runtime control`) with non-interactive `tabIndex -1` labels never entering the tab sequence, a flat link order identical to the pre-grouping sequence, correct `aria-current="page"`, zero empty group headings, and an identical Escape-closable 375 px menu producing zero requests on open, close, and traversal with one route activated per group; browser axe (`color-contrast` enabled, no rule group disabled) returned 0 serious and 0 critical (0 total) in all eleven tested states, closing UI-E2's long-open `page-has-heading-one` finding on both auth pages; and console, network, and storage hygiene held with 0 page errors, 0 warnings, one session-wide pre-login `401 /auth/session` probe, 0 requests on resize/theme/menu/traversal, and an `utcp.appearance` + `pusherTransportTLS`-only boundary. One recorded proof limitation: no restricted test identity exists that could be used without creating a user or editing roles, so deterministic `App.test.ts` coverage remains the authority for capability-empty group suppression, which is additionally structural since `visibleNavigationGroups()` derives groups from visible entries only. No product defect remains, all five UI-E completion criteria are satisfied, and UI-E is Complete. Deferred, non-blocking and outside UI-E closure: the `UiFilterBar` Apply busy-state limitation, and the T2/T3/V0-gated TelephonySession and listener-health views tracked under UI-D. |
| R0 Portfolio release                                         | Planned     | No release has been prepared.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

## Recent T3 Notes

T3-S1 is Complete. **T3-S2A live signaling proof is INCOMPLETE at `cbc098e`: `PRODUCT_DEFECT-6` is closed and confirmed live, but three new defects remain open — `PRODUCT_DEFECT-7`, `PRODUCT_DEFECT-8`, and `PRODUCT_DEFECT-9`.** The authenticated-dialog reproof recorded in `docs/evidence/t3/t3-s2a-asterisk-sip-application-dialog-live-proof.md` applied only the corrected `kamailio-config` ConfigMap (sha256 `bc14c98e...` to `749fc1ca...`) and the checksum-coupled `kamailio` Deployment (generation 12 to 13, adding the `sip-udp` UDP `5060` container port), producing a fully automatic ~5-second rollout to ReplicaSet revision 13 with no manual restart and no timestamp annotation. Proven live: Kamailio owns exactly one UDP `0.0.0.0:5060` socket (its own startup banner and the Pod socket table both confirm it) with no public Kamailio UDP surface, no HostPort, no HostNetwork, no UDP Service, and no node or developer-host socket; the authenticated INVITE now reaches healthy Asterisk over the canonical internal Service and the prior false healthy-Asterisk `503` is gone; Asterisk identifies the internal `anonymous` endpoint and executes `from-kamailio` extension `9900` through `Answer()` to `Echo()`, captured live as `PJSIP/anonymous-00000001 ... Prio 3 Up Echo`; a valid SDP-bearing `200 OK` returns through Kamailio carrying Asterisk's own origin, connection address, and RTP port with zero rtpengine rewriting and zero rtpengine sessions; `record_route()` executes with correct double record-routing; restoration after the intentional `asterisk-ari` `1 -> 0 -> 1` availability test is automatic, with the INVITE succeeding again against the new Pod IP and no stale address pinning; REGISTER and unsupported-method `405` are preserved; and no durable dialog or media authority, canonical state mutation, or unrelated workload restart occurred. **`PRODUCT_DEFECT-7`:** the `request_route` domain guard `if ($rd != "sip.utcp.local.test")` executes before `has_totag()` and `loose_route()`, so every in-dialog ACK and BYE — whose Request-URI is the negotiated Asterisk remote target `sip:<pod-ip>:5060` — is rejected `403 Forbidden` with `result=foreign_domain`; ACK never reaches Asterisk (which retransmits its `200 OK` and tears the channel down on its own timer) and BYE cannot terminate the dialog. **`PRODUCT_DEFECT-8`:** `record_route()` advertises `sip:0.0.0.0` for both hops because the listeners bind the wildcard address with no advertised address, yielding an unroutable route set; this is latent for the SIP-over-WSS client direction, which reuses its WebSocket transport, and blocking for Asterisk-initiated in-dialog requests. **`PRODUCT_DEFECT-9`:** `route[ASTERISK_RELAY]` detects unavailability only through the synchronous `t_relay()` return value and arms no failure route, so the committed `503 Application Runtime Unavailable` contract is unreachable — with `asterisk-sip` at zero Ready endpoints the authenticated INVITE received `100 Trying` then a bare `408 Request Timeout` after ~30 seconds with no `result=asterisk_unavailable` signal, while no second destination, Pod-IP fallback, ARI fallback, rtpengine routing, direct-media bypass, or RuntimeNode mutation occurred. CANCEL remains a bounded proof limitation because the `9900` fixture answers immediately. T3-S2A is In Progress, T3-S2 media mediation is Not Started, and T3 remains In Progress. `docs/evidence/t3/t3-s2a-asterisk-sip-application-dialog-authority.md` records the implemented internal Asterisk SIP authority: one canonical internal Asterisk SIP Service, explicit PJSIP UDP `5060` authority, exact reciprocal Kamailio/Asterisk SIP NetworkPolicies, a local-only `9900` media fixture, and an authenticated Kamailio application-dialog route seam without rtpengine operations or SDP rewriting. `PRODUCT_DEFECT-5` is corrected in the repository by `docs/evidence/t3/t3-s2a-kamailio-siputils-rendered-config-correction.md`: the canonical Kamailio ConfigMap now loads `siputils.so`; every supported rendered Kamailio configuration is extracted from the rendered ConfigMap and parsed with the pinned Kamailio image; deterministic `utcp.io/kamailio-config-sha256` Pod-template coupling replaces manual rollout restart dependence; and mutation coverage rejects missing modules, unknown commands, invalid module parameters, route syntax errors, rendered ConfigMap drift, stale checksums, and manual rollout timestamps. No Kubernetes apply or workload restart was performed by the correction. T3-S2A is ready for resumed Kamailio-only live proof, T3-S2 media mediation is Not Started, and T3 remains In Progress. Hosted GitHub Actions execution has not been observed for the uncommitted working tree. **The T3-S2A Kamailio dialog reproof at `92365f8` closes `PRODUCT_DEFECT-5` and is recorded in [`docs/evidence/t3/t3-s2a-asterisk-sip-application-dialog-live-proof.md`](../evidence/t3/t3-s2a-asterisk-sip-application-dialog-live-proof.md), but T3-S2A is ready for authenticated-dialog reproof after repository correction of `PRODUCT_DEFECT-6`.** Proven live: only the corrected `kamailio-config` ConfigMap (sha256 `a4b733fd...` to `bc14c98e...`) and the checksum-coupled `kamailio` Deployment (generation 11 to 12) were applied; the `utcp.io/kamailio-config-sha256` annotation equals the rendered `kamailio.cfg` SHA-256 exactly, so configuration content alone changed the Pod template and produced a fully automatic ~3-second rollout with **no manual restart and no timestamp annotation**; ReplicaSet convergence was correct (new revision 12 took over, the old Ready Pod was retired only after the replacement reached Ready, and the previously failing revision 11 no longer owns a replica); `siputils.so` loads, the authoritative parser run against the exact running configuration is clean with no `has_totag` error, and the running configuration is byte-identical across repository render, live ConfigMap, in-Pod mount, and Pod annotation; the old blanket non-REGISTER `405` guard no longer intercepts INVITE while the REGISTER branch is unchanged and no rtpengine operation or SDP rewriting exists; an unauthenticated INVITE to `9900` now returns the canonical `401` challenge with `realm=sip.utcp.local.test` instead of the previous `405`; subscriber digest authentication for INVITE **succeeds** (execution reached `route[ASTERISK_RELAY]`, which is gated behind the authentication guard) using a credential issued only through the authorized API; `MESSAGE` still returns `405` from the corrected configuration; REGISTER is preserved end to end (`200 accepted`, one active location contact, registrar branch taken, Asterisk and rtpengine untouched); rtpengine sessions and port counters are unchanged with zero offer/answer/delete operations; and Asterisk, rtpengine, and every unrelated workload retain identical Pod UIDs and restart counts with no canonical state mutation. **`PRODUCT_DEFECT-6`:** `kamailio-configmap.yaml` declares only `listen=tcp:0.0.0.0:8080` and provides no UDP socket, while `route[ASTERISK_RELAY]` targets `sip:asterisk-sip.utcp-runtime.svc.cluster.local:5060;transport=udp`. `t_relay()` therefore cannot add a branch (`uri2dst2(): no corresponding socket found ... (udp:10.43.209.141:5060)`, `prepare_new_uac(): can't fwd ... (no corresponding listening socket)`) and the committed `503 Application Runtime Unavailable` branch fires **while Asterisk is healthy and its Service endpoint is Ready** — a false unavailability signal masking a transport misconfiguration. Static and rendered-parser checks pass because syntactic validity does not imply a matching transport socket. [`docs/evidence/t3/t3-s2a-kamailio-udp-relay-socket-correction.md`](../evidence/t3/t3-s2a-kamailio-udp-relay-socket-correction.md) records the repository correction: the canonical ConfigMap now adds exactly one `listen=udp:0.0.0.0:5060` socket beside the existing TCP `8080` listener, the Deployment declares the matching UDP `5060` container port, the deterministic `utcp.io/kamailio-config-sha256` annotation is recalculated from the rendered `kamailio.cfg`, and `scripts/kamailio-signaling/config-check` now asserts that every routing destination transport has a corresponding `listen=` socket and matching container port. Consequently the relayed INVITE, Asterisk execution of `9900`, the SDP-bearing answer, `Record-Route`, ACK, BYE, and a genuine Asterisk-unavailable `503` remain unproven and are not claimed; the Asterisk-unavailable condition was deliberately not induced because the same `503` already fires with Asterisk healthy and would produce non-discriminating evidence. T3-S2 media mediation is Not Started and T3 remains In Progress.

**T3-S2A live signaling proof is INCOMPLETE at `081267a`: `PRODUCT_DEFECT-7`, `PRODUCT_DEFECT-8`, and `PRODUCT_DEFECT-9` are all closed and confirmed live, and one new defect — `PRODUCT_DEFECT-10` — remains open.** The final in-dialog reproof recorded in `docs/evidence/t3/t3-s2a-asterisk-sip-application-dialog-live-proof.md` applied exactly five resources in dependency order — the corrected `kamailio-config` ConfigMap (sha256 `749fc1ca...` to `58e5c733...`), the new ClusterIP `kamailio-sip-internal` Service (UDP `5060`, `targetPort: sip-udp`), both reverse NetworkPolicies, and the checksum-coupled `kamailio` Deployment (generation 13 to 14) — producing a fully automatic ~4-second rollout to ReplicaSet revision 14 with no manual restart. Proven live: `Record-Route` now advertises `sip:kamailio-sip-internal.utcp-platform.svc.cluster.local:5060` and `sip:sip.utcp.local.test:443;transport=ws` with no `0.0.0.0`, Pod IP, node IP, or developer-host IP anywhere in the route set; in-dialog ACK is loose-routed so Asterisk post-ACK `200 OK` retransmissions fell from 3 to 0; the client-originated BYE returns `200 OK` and terminates the channel with no manual cleanup; the internal Service carries exactly one Ready endpoint equal to the current Kamailio Pod IP; the reverse corridor admits only the canonical workloads on UDP `5060` in both directions with default-deny intact, no `ipBlock`, no namespace-wide SIP rule, no media ports, and the secondary `asterisk-ari-b` workload correctly excluded (a bounded probe from the real Asterisk network identity to the internal Service returned `200 Keepalive` while a non-admitted destination timed out); a fresh out-of-dialog request to a non-canonical domain still returns `403` with `result=initial_foreign_domain` and reaches neither Asterisk nor rtpengine; zero Ready Asterisk endpoints now produce an explicit `503 Application Runtime Unavailable` through `failure_route[ASTERISK_UNAVAILABLE]` with a Call-ID-correlated `result=asterisk_unavailable` log, replacing the previous bare `408`, with no alternate workload, Pod-IP fallback, ARI route, rtpengine route, direct-media bypass, second destination, or RuntimeNode mutation; restoration is automatic with INVITE, ACK, and BYE all succeeding against the new Asterisk Pod IP; and REGISTER, unsupported-method `405`, rtpengine non-involvement, public-surface containment, and state authority are all preserved with no unrelated workload restart. **`PRODUCT_DEFECT-10`:** the canonical Asterisk workload has UDP `5060` egress to Kamailio but **no DNS egress** — `utcp-runtime/default-deny` denies all other egress and no policy grants the Asterisk Pod UDP/TCP `53`, so every name lookup fails (`gaierror`) and a raw query to `kube-dns` `10.43.0.10:53` times out. Because closing `PRODUCT_DEFECT-8` made `record_route()` advertise a DNS name, Asterisk cannot resolve its own route set, so Asterisk-originated in-dialog requests (BYE on hangup, session-timer re-INVITE) never leave the Pod: a requested hangup of a confirmed proof channel produced zero Kamailio log lines and the client never received a BYE. Kamailio already has the mirror DNS rule (`UDP 53` + `TCP 53` to `kube-system`); the Asterisk policy lacks it. The smallest correction is that mirrored rule plus static coverage asserting DNS egress for any workload required to reach an advertised in-cluster identity. CANCEL remains a bounded proof limitation because the `9900` fixture answers immediately. T3-S2A is In Progress, T3-S2 media mediation is Not Started, and T3 remains In Progress. `docs/evidence/t3/t3-s2a-in-dialog-routing-and-failure-contract-correction.md` records `PRODUCT_DEFECT-7 = corrected in repository`, `PRODUCT_DEFECT-8 = corrected in repository`, and `PRODUCT_DEFECT-9 = corrected in repository`: established-dialog routing now precedes initial-domain validation, Record-Route uses stable client-facing and internal Kamailio SIP Service advertisements, exact reciprocal Asterisk/Kamailio UDP `5060` NetworkPolicies render without public SIP exposure, and generated Asterisk no-response `408` is mapped to `503 Application Runtime Unavailable` by one failure route without retry, fallback, ARI, rtpengine, or broad response override. Static and mutation coverage guards the three defects; all supported Kamailio renders parse through the pinned image; no Kubernetes apply was performed. T3-S2 media mediation is Not Started. T3 remains In Progress. Hosted GitHub Actions execution has not been observed for the uncommitted working tree.

`PRODUCT_DEFECT-10 = corrected in repository` by `docs/evidence/t3/t3-s2a-asterisk-cluster-dns-egress-correction.md`: the canonical Asterisk policy now adds only the mirrored cluster-DNS egress corridor (`kube-system` plus `k8s-app=kube-dns`, UDP `53` and TCP `53`) while preserving exact UDP `5060` SIP egress to canonical Kamailio, secondary Asterisk exclusion, default-deny, public-surface containment, and the internal Kamailio Service DNS route-set identity. Static validation and mutation coverage now reject missing DNS, widened DNS, split selectors, `ipBlock`, broad CIDRs, unrestricted egress, secondary-workload inclusion, SIP-rule drift, and IP-literal route-set replacement. No Kubernetes apply was performed. T3-S2A is ready for final Asterisk-originated BYE reproof. T3-S2 media mediation is Not Started. T3 remains In Progress. **The T3-S2A Asterisk-originated BYE closure proof at `741efbb` closes `PRODUCT_DEFECT-10`, but T3-S2A remains In Progress behind a new `PRODUCT_DEFECT-11`.** Exactly one resource was applied — `NetworkPolicy/utcp-runtime/allow-asterisk-sip-from-kamailio` (generation 2 to 3, rv 445437 to 448277), whose `kubectl diff` contained only the added `UDP 53` + `TCP 53` egress rule to the `kube-system` namespace with `podSelector k8s-app=kube-dns` — and no workload was restarted or replaced (the full-cluster Pod snapshot diff is empty). The repository's matching tightening of the **Kamailio** DNS rule was proven by `kubectl diff` to be a pure hardening delta on an already-working rule and was deliberately not applied, leaving `allow-kamailio-signaling-required-traffic` live at generation 5 against a repository render of 6. Proven live: the canonical Asterisk workload now resolves cluster DNS from its real Pod network namespace over **both** UDP and TCP (`kamailio-sip-internal.utcp-platform.svc.cluster.local`, `rcode=0`, `10.43.3.212`, matching the live ClusterIP exactly, at 0.5 ms and 0.3 ms) through the in-cluster resolver `10.43.0.10` with no `/etc/hosts` entry and no `hostAliases` supplying the answer; the policy selects only the canonical `local-asterisk-ari` Pod while `asterisk-ari-b` is selected solely by `default-deny` and therefore holds no egress grant; existing SIP egress remains exact (a bounded probe resolving the Service by name received `SIP/2.0 200 Keepalive` from UDP `5060`, while rtpengine `ng` `2223` and PostgreSQL `5432` from the same identity remained denied); and **a real Asterisk-originated in-dialog BYE was generated by a bounded `channel request hangup` stimulus, left the Asterisk Pod, reached Kamailio over the internal Service identity, entered established-dialog handling and was processed by `loose_route()`** — Kamailio's own log carries the exact dialog Call-ID `1281477efeaeaf32@utcp-s2a-closure` with `route=within_dialog method=BYE`, with no `result=initial_foreign_domain` rejection and no repeated subscriber authentication. Default-deny remains active in both namespaces with no `ipBlock`, no unrestricted rule, no namespace-only destination, and no NodePort, LoadBalancer, ExternalIP, HostPort, HostNetwork, Gateway, Ingress, UDPRoute, or public SIP path added; rtpengine sessions and ports_used remained `0` with zero offer/answer/delete; and tables 41, tenants 27, RuntimeNodes 110, pending outbox 0 and Redis `sip`/`dialog`/`rtp`/`media` all 0 were preserved. **`PRODUCT_DEFECT-11`:** Kamailio cannot route an in-dialog request *to* a SIP-over-WSS client. `route[APPLICATION_DIALOG]` calls `record_route()` but never `set_contact_alias()`, and `route[WITHINDLG]` calls `loose_route()` then `t_relay()` but never `handle_ruri_alias()`; `nathelper.so` is loaded yet `set_contact_alias`, `add_contact_alias`, `handle_ruri_alias`, `fix_nated_contact`, `fix_nated_register` and `nat_uac_test` all occur **0** times. Kamailio therefore DNS-resolves the client's `Contact` host — which RFC 7118 section 5.2.1 requires to be a randomly generated `.invalid` domain, as every real browser stack uses — and fails: `sip_hostport2su(): could not resolve hostname: "utcp-s2a-proof.invalid"` then `uri2dst2(): failed to resolve` then `t_forward_nonack(): failure to add branches` then `sl_reply_error()`. The BYE never reaches the client, the client returns no response, and the Asterisk channel terminates only by its own local hangup. The seam is isolated precisely by an asymmetry: *responses* toward the client still work, because `tm` routes them by `Via` on the transaction-bound WebSocket connection, so only a **new request** toward the client needs the missing alias binding. This blocks any Asterisk-initiated in-dialog request toward a browser client — BYE on hangup, session-timer re-INVITE, in-dialog UPDATE — and therefore directly blocks V0 conference admission semantics. All static checks pass because nothing asserts that the WebSocket leg has a transport binding usable for server-initiated requests. The smallest bounded correction is `set_contact_alias()` before `record_route()` on the initial client INVITE plus `handle_ruri_alias()` after `loose_route()` and before `t_relay()`, with matching `scripts/kamailio-signaling/config-check` and `config-check-test` coverage; aliasing the REGISTER-stored contact belongs to the separate V0 inbound-routing slice. CANCEL remains a bounded proof limitation because the `9900` fixture answers immediately. T3-S2A is In Progress, T3-S2 media mediation is Not Started, and T3 remains In Progress.

## Recent UI-D Notes

- UI-D11 repository correction (evidence `docs/evidence/ui/ui-d11-echo-subscription-readiness-fix.md`) removes the unsupported Echo channel subscription lifecycle `bind` path and uses `subscribed()` / `error()` for RuntimeNode and Conference subscription readiness. Reconnect resynchronization is generation-fenced and idempotent, tenant switches can clear stale state after the new snapshot and channel subscription without another socket event, and logout/session interruption invalidates late callbacks. UI-D remains In Progress pending the focused natural Playwright MCP proof of reconnect resynchronization, RuntimeNode tenant-switch readiness, Conference tenant isolation, previous-tenant event rejection, and logout interruption.
- UI-D13 repository implementation (evidence `docs/evidence/ui/ui-d13-runtime-operation-read-api.md`) adds read-only canonical Runtime Operation list and detail APIs using the existing `runtime_operations` table, active-tenant scoping, `runtime.nodes.view`, deterministic pagination, validated stored-field filters, explicit safe response resources, bounded RuntimeNode and reconciliation references, and sensitive-field exclusions. UI-D remains In Progress for the bounded frontend Runtime Operations operational view and separate audit/reconciliation read APIs.
- UI-D14 repository implementation (evidence `docs/evidence/ui/ui-d14-runtime-operation-live-implementation.md`) adds the read-only Runtime Operations operational route `/operations/runtime-operations`, canonical list/detail API consumption, backend pagination and stored-field filters, selected-detail-on-demand loading, the tenant private channel `tenant.{tenantId}.runtime-operations` authorized by active session tenant plus `runtime.nodes.view`, metadata-only queued notifications for `runtime_operation.*` outbox rows, shared Echo-client subscription lifecycle, scoped canonical rereads for selected and unrelated operation events, reconnect resynchronization, tenant-switch/logout cleanup, request-budget tests, storage-boundary tests, and responsive hygiene coverage. UI-D remains In Progress pending natural Playwright MCP proof of the Runtime Operations surface and separate audit/reconciliation surfaces.
- UI-D15 controlled natural live proof (evidence `docs/evidence/ui/ui-d15-runtime-operation-live-proof.md`, api `sha256:d06905de…` / web `sha256:d2a3da08…` from `9977e3e`) **is blocked and does not prove the UI-D14 surface**. Proven: image provenance, six healthy workloads (api, worker, control-plane-outbox-dispatcher, reverb, gateway, web), migration Complete on `BROADCAST_CONNECTION=log` with no Reverb credentials, Reverb ClusterIP-only `8080` with browser WSS on Traefik `443` and Redis `6379` open, a canonically repaired stale Kubernetes API egress pin (`172.24.0.3/32` → `172.24.0.5/32` via `scripts/security/render-apiserver-policy`, no manual edit, no Traefik restart), natural break-glass login with forced password change and Local Tenant selection, capability-gated AppShell navigation to `/operations/runtime-operations` with no URL bypass, a read-only route exposing only Refresh/Apply/Clear/Details, exactly 1 list request and 0 detail requests on entry with no per-operation scaling and no WebSocket-supplied state, an unregressed shared realtime foundation (one socket `wss://app.utcp.local.test/app/<public-key>`, `POST /api/broadcasting/auth` 200, `private-tenant.{tenantId}.runtime-nodes` subscribed, 1 catalog + 1 list + 0 detail fan-out), 0 new sockets on route change, 0 requests and 0 reconnects on theme change, clean logout (`POST /api/v1/auth/logout` 200, 0 post-logout broadcasting-auth, 0 post-logout Runtime Operation requests, real Login page), a storage boundary of only `utcp.appearance` plus the bounded `pusherTransportTLS` vendor cache, and zero 375px overflow in Light and Dark. **Blocker 1:** `RuntimeOperationQuery::baseQuery()` joins `runtime_nodes.id`/`.tenant_id` and `runtime_reconciliation_states.tenant_id` (PostgreSQL `uuid`) against `runtime_operations.runtime_node_id`/`.tenant_id` (`character varying`), so `GET /api/v1/admin/runtime-operations` and `…/{id}` both return HTTP 500 with `SQLSTATE[42883]: operator does not exist: uuid = character varying`; the route shows an explicit “Runtime Operations unavailable / Server Error” alert, renders no rows, and never subscribes because subscription is correctly gated behind a successful canonical snapshot. `apps/api/phpunit.xml` runs on SQLite, where `$table->uuid()` is `varchar`, so all 26 focused and 376 total backend tests pass while the canonical PostgreSQL deployment fails every request. **Blocker 2:** no production path ever appends an outbox row with `aggregate_type = 'runtime_operation'` — the live outbox holds 4207 `runtime_node`, 1072 `conference_participant`, 881 `conference`, 338 `telephony_session`, and 0 `runtime_operation` — so `OperationalBroadcastBridge::dispatchRuntimeOperationNotification()` and the channel `tenant.{tenantId}.runtime-operations` are unreachable; `runtime_operation.*` event types are emitted under `aggregate_type = runtime_node`/`conference` and are rejected by every bridge branch, and `RuntimeOperationRealtimeBroadcastTest` only passes because it synthesises the row. Pagination, filters, page/filter preservation, selected-detail budget, broadcast envelope, unrelated- and selected-operation rereads, reconnect resynchronization, route-leave, tenant-switch isolation, and previous-tenant rejection remain unproven and were not fabricated. Next: bounded implementation of both blockers plus PostgreSQL-enforcing coverage for `RuntimeOperationQuery`. UI-D remains In Progress.
- UI-D16 repository correction (evidence `docs/evidence/ui/ui-d16-runtime-operation-postgres-outbox-fix.md`) converts Runtime Operation tenant and nullable RuntimeNode references to canonical PostgreSQL UUID types after validating existing values, proves Runtime Operation list/detail reads against PostgreSQL through the existing control-plane test target, adds transactional `runtime_operation.created` and `runtime_operation.status_changed` outbox production in `RuntimeOperationRepository`, replaces the main realtime acceptance test with producer-backed coverage, preserves the metadata-only browser notification envelope and strict bridge boundary, and enforces the rendered broadcaster role split so API/worker/outbox-dispatcher/Reverb are Reverb publishers while scheduler, telephony workers, simulator event source, runtime fence worker, Kamailio/Asterisk observers, and migration use `BROADCAST_CONNECTION=log` without Reverb credentials. UI-D remains In Progress pending the corrected natural Runtime Operations live proof and separate audit/reconciliation surfaces.
- UI-D17 controlled natural live proof (evidence `docs/evidence/ui/ui-d17-runtime-operation-corrected-live-proof.md`, api in-cluster `sha256:7c7c1db9…` from registry manifest `sha256:d4284d67…` built from `39ea898`; web deliberately unchanged at `sha256:d2a3da08…` since `git diff 9977e3e..39ea898 -- apps/web` is empty) **closes both UI-D15 blockers and proves the Runtime Operations surface end to end**. Pre-migration read-only validation: 4714 rows, 4714 non-null tenant_id and runtime_node_id, 0 malformed UUIDs, 0 unresolved tenant or RuntimeNode references. The committed migration converted `runtime_operations.tenant_id` and nullable `runtime_operations.runtime_node_id` to `uuid` in 179.33 ms with all 4714 rows and all five indexes preserved, executed by the canonical migration Job on `BROADCAST_CONNECTION=log` with no Reverb credentials and no schema SQL outside the migration. Live schema is `tenant_id uuid NULL` / `runtime_node_id uuid NULL`; the three canonical joins each resolve 4714 rows with no added casts; API logs contain 0 `SQLSTATE[42883]` and 0 `uuid = character varying`. `make k8s-apply` completed cleanly with no rollout-wait failure (UI-D15 aborted on `scheduler`). Publishers `api`/`worker`/`control-plane-outbox-dispatcher`/`reverb` all use `reverb` with full credentials and canonical host/port/scheme; the nine classified non-publishers plus the migration Job use `log` with credentials absent; a `null auth_key`/`BroadcastManager` scan over all 15 Deployments returned 0, and the nine workloads that had 38–45 restarts each under UI-D15 now run 1/1. `utcp-runtime-fence-worker` is staged out of the canonical local overlay by design (`scripts/telephony-domain/config-check:188`) so its committed component manifest was applied directly. Browser proof from the real Login page with cookies and storage cleared first: capability-gated navigation, read-only route, canonical list and detail both **200** with the UI-D15 error alert absent; 1 list and 0 detail requests on entry over 4714 operations, channel auth 200 at +184 ms and `private-tenant.{tenantId}.runtime-operations` subscribed at +194 ms on one socket; server-backed pagination with exactly 1 list request per page change, 0 detail requests, and deterministic ordering across page sizes; all six exposed filters mapping to exact canonical query parameters with page reset to 1 (`status=terminal_failed`→55 rows, `operation_type=conference.close`→159, correlation-id and bounded created_from/created_to→1 each) and correct clearing; a safely invalid `correlation_id` returning **422** presented as "The correlation id field format is invalid." rather than ignored; selection issuing exactly 1 detail request for a 21-key safe contract with no raw payload, idempotency key, lease, failure message, trace, outbox body, audit payload, provider response, credential, secret, endpoint or environment value. **Canonical notification corridor proven with production events**: `aggregate_type='runtime_operation'` outbox rows went 0 → 36, written transactionally by `RuntimeOperationRepository` transitions with payload bounded to `runtime_operation_id` plus `runtime_node_id`; browser envelopes carried exactly the seven approved keys with `aggregate_type=runtime_operation`, `aggregate_id=runtime_operation_id`, the active `tenant_id`, and no status field, so the stream cannot become authority. Unrelated-operation events produced list-only rereads with 0 selected-detail and 0 other detail requests. A deterministic selected-operation proof (dedicated `telephony-command-worker` scaled to 0 while reconciler, outbox dispatcher, queue worker, web and Reverb stayed up, with 0 pending operations beforehand) yielded exactly 1 list reread per event and 1 selected-detail reread per selected-operation event with 0 unrelated details, and final status/timestamps read from the detail HTTP response; every reread preserved `?runtime_node_id=…&page=1&per_page=10`. Reconnect over a Reverb scale-to-zero preserved data with a stale badge and 0 rereads during the outage, then performed exactly one bounded resync (1 list + 1 detail + 0 unrelated) with page, filters and selection preserved and stale cleared after the HTTP rereads. Route leave sent `pusher:unsubscribe`, kept the shared socket for the Conferences channel, and produced 0 Runtime Operation requests or frames afterwards; returning issued 1 fresh list request and 1 fresh subscription with 0 new sockets. Tenant switch left the old channel, cleared the selection, loaded the new tenant exactly once with 0 initial detail requests, 0 leaked rows and 0 new sockets; a second independent context that created and executed a Local Tenant operation produced 0 accepted events, 0 rereads and 0 leaked rows in the primary held on Proof Tenant. Logout closed the socket with 0 post-logout broadcasting-auth and 0 Runtime Operation requests. Storage stayed bounded to `utcp.appearance` plus the metadata-only `pusherTransportTLS`; 375px overflow was 0 in Light and Dark with the selected detail inside the viewport, long identifiers wrapping, and status conveyed as text rather than colour alone. Cleanup restored all five disposable simulator RuntimeNodes, the scaled `telephony-command-worker` and `scheduler`, and the Reverb replica count; all 6557 outbox rows are dispatched with 0 non-terminal operations and Runtime Operation history preserved. One divergence is recorded and classified: the first selected-operation attempt lost its measurement to an observer buffer clear (proof-harness error, not a product defect) and was re-proven cleanly with timestamp-based phase separation. UI-D remains In Progress for the reconciliation-record and audit read authorities.
- UI-D18 repository implementation (evidence `docs/evidence/ui/ui-d18-runtime-reconciliation-read-api.md`) adds read-only Runtime Reconciliation list and detail APIs using the existing `runtime_reconciliation_states` current-state table, active-tenant scoping, `runtime.nodes.view`, deterministic pagination, validated stored-field filters, explicit safe resources, bounded RuntimeNode and Runtime Operation references, safe failure redaction, desired/observed payload exclusions, sensitive-field exclusions, no N+1 list behavior, and PostgreSQL endpoint coverage through the established control-plane test target. UI-D remains In Progress pending the bounded Runtime Reconciliation operational view and separate Audit API.
- UI-D19 repository implementation (evidence `docs/evidence/ui/ui-d19-runtime-reconciliation-live-implementation.md`) adds the read-only Runtime Reconciliation operational route `/operations/runtime-reconciliations`, canonical list/detail API consumption, backend pagination and stored-field filters, selected-detail-on-demand loading, the tenant private channel `tenant.{tenantId}.runtime-reconciliations` authorized by active session tenant plus `runtime.nodes.view`, transactional `runtime_reconciliation.*` aggregate outbox production from `ReconciliationRepository`, metadata-only queued notifications, shared Echo-client subscription lifecycle, scoped canonical rereads for selected and unrelated reconciliation events, reconnect resynchronization, tenant-switch/logout cleanup, producer-backed broadcast tests, request-budget tests, storage-boundary tests, and responsive hygiene coverage. UI-D remains In Progress pending natural Playwright MCP proof of the Runtime Reconciliation surface and the separate Audit API.
- UI-D20 controlled natural live proof (evidence `docs/evidence/ui/ui-d20-runtime-reconciliation-live-proof.md`, api `sha256:5ff2e330…` / web `sha256:5947ec57…` built from `d9fe346` with `org.opencontainers.image.revision=d9fe346`) **is blocked and does not complete the UI-D19 surface**. All 12 API-image Deployments plus web were enumerated from the rendered manifests and explicitly restarted (with `crictl rmi` on all three k3d nodes to defeat the fence worker's `IfNotPresent` policy), giving zero version skew — 13 API pods on one digest, 1 web pod on one digest — and `make k8s-apply` completed with migration `Complete` on `BROADCAST_CONNECTION=log` without Reverb credentials. **Proven:** capability-gated navigation and a read-only route with no reconcile/retry/replay/repair/clear-drift/delete/approve/manual-sync control; canonical list and detail both 200 with PostgreSQL joins succeeding and pagination metadata present; a 15-key safe contract with zero raw desired/observed state, credentials, adapter configuration, endpoint secrets, provider responses, commands, traces, audit or outbox payloads (generations exposed only as integers); server-backed pagination with 1 list request per page change, 0 detail requests and deterministic `updated_at desc, id desc` ordering; all six filters (`runtime_node_id`, `status`, `target_type`, `runtime_operation_id`, `updated_from`, `updated_to`) mapping to exact canonical parameters with page reset to 1 and correct clearing; a 422 validation message surfaced rather than ignored; 1 detail request per selection with 0 unrelated detail requests; production outbox rows rising 0 → 4560 from real `ReconciliationRepository` transitions; a browser envelope whose key union across 60 consecutive frames is exactly `aggregate_id, aggregate_type, event_type, occurred_at, runtime_reconciliation_id, tenant_id` with `aggregate_type=runtime_reconciliation`, `aggregate_id === runtime_reconciliation_id` and the active tenant in 100 % of frames and **zero** status/generation/drift/failure/credential keys; unrelated events causing list-only rereads (60 events → 60 list rereads, 0 detail); reconnect performing exactly one bounded resync (1 list + 1 detail + 0 unrelated) with query, selection and stale-clearing all correct; route leave sending `pusher:unsubscribe`, keeping the shared socket and issuing 0 reconciliation requests for 12 s afterwards, then 1 fresh list + 1 fresh subscription on return with 0 new sockets; tenant switch leaving the old channel, clearing selection, loading once with 0 detail requests and 0 leaked rows; a second independent context receiving 210 Local Tenant events over 60 s while the primary on Proof Tenant accepted **0** events, 0 rereads and 0 leaked rows; clean logout with 0 post-logout auth or reconciliation requests; storage bounded to `utcp.appearance` plus metadata-only `pusherTransportTLS`; and 0 overflow at 375px in Light and Dark with drift and status expressed as text. **Blocking defect:** `ReconciliationRepository` emits outbox events for reconciliation *non-transitions* — `claimDue()` appends `runtime_reconciliation.reconciliation_started` for every claimed row on every poll and `markResult()` appends an event for every result, mapping the steady-state `waiting` status to `retry_scheduled`, neither comparing prior and resulting state. With 460 reconciliation states the reconciler therefore produces a permanent ~7 event/second storm in which 98.4 % of events (2280 `reconciliation_started` + 2206 `retry_scheduled` against 72 `converged` + 2 `failed`) carry no state change and 200/200 sampled reconciler results are `waiting` no-ops. Measured consequences: the route issued 11 list requests on entry and then **110 canonical list rereads in a 30-second idle window with no user action** (3.67/s, exactly 1 per event — the client contract is correct, the event volume is not); and the canonical outbox dispatch backlog grew monotonically from 0 to 1000+ (production ~7 rows/s versus a measured dispatcher drain of ~3.3 rows/s), ending at 700 pending and still climbing. Repository coverage cannot see this because it asserts that a transition produces an event but never that a non-transition produces none. Request-discipline measurements above were therefore taken in an isolated quiescent window with the reconciler and scheduler at zero replicas and the outbox drained, which is stated explicitly in the evidence; the selected-reconciliation reread could not be isolated at all and that exact canonical timing limitation is preserved rather than fabricated. Next: bounded correction to emit only on real state change, plus a no-op-reconcile regression test. UI-D remains In Progress.
- UI-D21 repository correction (evidence `docs/evidence/ui/ui-d21-reconciliation-noop-event-suppression.md`) suppresses false Runtime Reconciliation lifecycle events at the producer: routine `claimDue()` lease changes now emit zero events, `markResult()` compares the prior and resulting canonical fingerprint (`status`, `desired_generation`, `observed_generation`, `last_operation_id`, `blocked_reason`) before appending any outbox row, repeated no-op waiting/failure/retry/converged/operation-required results remain silent, and real convergence, drift, new linked operation, failure or blocked-reason change, and retry-state transitions still emit exactly one `runtime_reconciliation.*` aggregate event transactionally. Tests cover no-op claim/result cycles, claim-only cycles, 20 stable rows over 5 polling cycles, PostgreSQL fencing and rollback behavior, and producer-backed broadcast preservation. Browser channel/envelope contracts are unchanged, no downstream debounce or throttling was added, automatic reconciliation remains enabled, and historical outbox backlog is preserved for canonical drain. UI-D remains In Progress pending the corrected Runtime Reconciliation live proof after deployment and backlog drain, plus the separate Audit API.
- UI-D22 controlled natural live proof (evidence `docs/evidence/ui/ui-d22-reconciliation-quiescence-live-proof.md`, api `sha256:a3c8c6ce…` built from `5e31943` with `org.opencontainers.image.revision=5e31943`; web unchanged at `sha256:5947ec57…` / `revision=d9fe346`) **closes the UI-D20 event-storm blocker and completes reconciliation live acceptance**. All 12 API-image Deployments plus web were enumerated from the rendered manifests and explicitly restarted (with `crictl rmi` on all three k3d nodes for the `IfNotPresent` fence worker), giving zero skew — 13 API pods on the `5e31943` digest, 1 web pod on the proven `d9fe346` bundle — and migration completed on `BROADCAST_CONNECTION=log` without Reverb credentials. **No-op production stopped**: with scheduler and reconciler at normal replicas, `rr_total` froze at 28101 immediately after rollout while the preserved UI-D20 backlog drained canonically 12240 → **0** over ~57 min (115 samples, ~3.4/s dispatch, 0 failed, no row deleted or manually marked); +60 reconciler claims in 20 s and 200/200 `waiting` results produced 0 outbox rows. **Primary browser quiescence**: 150 s / ~50 poll cycles / +350 claims on the mounted route with a selection held → 0 events, 0 list rereads, 0 detail rereads (UI-D20 saw 110 idle rereads in 30 s); route entry issued exactly 1 list request (was 11). **Each real fingerprint transition still emits exactly once**: an unrelated node Activate produced 3 fingerprint changes → +3 outbox rows → 3 notifications → 3 list rereads → 0 selected-detail rereads; the selected node produced 5 changes (drift → 3 distinct operation_required, each with a distinct real `runtime_operation_id`, → converged) → +5 rows → 5 list + 5 selected-detail rereads → 0 unrelated, with status/generations/operation/failure all read from the detail HTTP response and the envelope carrying only the eight approved metadata keys with no status/generation/drift/state/failure/credential data; 110 s of subsequent polling with the row selected emitted 0 repeat events. Regressions held: reconnect performed one bounded resync (1 list + 1 detail + 0 unrelated, page/filters/selection preserved); route leave sent `pusher:unsubscribe`, kept the shared socket, issued 0 reconciliation requests, then 1 fresh list + 1 subscription on return; tenant switch left the old channel, cleared selection, loaded once with 0 detail requests, 0 new sockets and 0 leaked rows; a second context's real Local Tenant transition (server-confirmed 3 outbox rows on the `local` channel) yielded 0 accepted events / 0 rereads / 0 leaked rows in the primary on Proof Tenant; logout closed the socket with 0 post-logout auth or reconciliation requests; storage stayed bounded to `utcp.appearance` + `pusherTransportTLS` with a zero-hit forbidden-substring scan; and 375px overflow was 0 in Light and Dark. All three transitioned disposable RuntimeNodes were restored to disabled, Reverb (cycled only for the reconnect regression) restored to 1, and pending outbox confirmed stable at 0 across a final idle window. UI-D remains In Progress for the canonical Audit read authority.
- UI-D23 repository implementation (evidence `docs/evidence/ui/ui-d23-audit-read-api.md`) adds tenant-scoped read-only Audit list and detail APIs backed by the existing append-only `control_plane_audit_records` table and `AuditRepository` append authority, reusing `tenant.memberships.manage` as the existing tenant-admin capability. The APIs preserve PostgreSQL append-only immutability, use SQL-backed pagination, deterministic `occurred_at desc, id desc` ordering, validated stored-column filters, explicit safe resources, bounded actor and subject references, safe outcome derivation, redacted metadata only, no sensitive payload exposure, no write/delete/replay/retention controls, non-scaling list query coverage, and PostgreSQL endpoint coverage through the established control-plane target. UI-D remains In Progress pending the bounded Audit operational view and a repository-evidence decision on whether Audit requires realtime notifications.
- UI-D24 repository implementation (evidence `docs/evidence/ui/ui-d24-audit-view-implementation.md`) adds the read-only `/admin/audit-records` operational view on the canonical Audit list/detail HTTP APIs, gated by active tenant plus `tenant.memberships.manage`. It covers server-backed pagination, canonical filters, explicit refresh without polling or timers, selected-detail-on-demand loading, bounded actor/subject/outcome/reason/safe-metadata rendering, sensitive exclusion tests, no Audit storage, and responsive hygiene. Audit realtime notifications remain deliberately unimplemented, and UI-D remains In Progress pending focused natural Playwright MCP proof and final UI-D completion assessment.
- T3-S2A repository correction (evidence `docs/evidence/t3/t3-s2a-websocket-dialog-alias-correction.md`) records `PRODUCT_DEFECT-11 = corrected in repository`: authenticated initial WS/WSS application INVITEs now use `add_contact_alias()` after subscriber authentication and before `record_route()`/Asterisk relay, established-dialog routing now consumes aliases with `$du == ""` plus `handle_ruri_alias()` after `loose_route()` and before relay, alias failures return `400 Bad Request` and stop, REGISTER remains unchanged, no Outbound/Path/GRUU, rtpengine, public SIP, or second dialog authority was added, and static plus mutation coverage guards the contract. T3-S2A = ready for final browser-bound BYE reproof. T3-S2 media mediation = Not Started. T3 = In Progress. **The T3-S2A WebSocket-bound BYE proof at `b547a98` did NOT close `PRODUCT_DEFECT-11`; two new exact defects, `PRODUCT_DEFECT-12` and `PRODUCT_DEFECT-13`, are open.** Exactly three resources were applied in dependency order — `allow-kamailio-signaling-required-traffic` (generation 5 to 6, only the previously reviewed cluster-DNS `podSelector k8s-app=kube-dns` hardening, with the DNS, PostgreSQL, rtpengine and Asterisk egress authorities all intact and no `ipBlock`), the corrected `kamailio-config` ConfigMap (sha256 `58e5c733...` to `3a38ad30...`) and the checksum-coupled `kamailio` Deployment (generation 14 to 15) — producing a fully automatic ~4-second rollout to ReplicaSet revision 15 with no manual restart and no unrelated workload rollout. The running configuration is byte-identical across repository render, live ConfigMap, in-Pod mount and Pod checksum annotation, with `add_contact_alias()` correctly placed after authentication and before `record_route()`, `handle_ruri_alias()` after `loose_route()` and before relay, zero alias operations in REGISTER and zero rtpengine operations. A bounded packet capture taken in the Kamailio k3d node network namespace, filtered to the single proof Call-ID with Authorization redacted, proves the outcome directly. **`PRODUCT_DEFECT-12`:** the guard `if ($proto == "WS" || $proto == "WSS")` compares against uppercase literals while Kamailio's `$proto` renders lowercase (`ws`/`wss`), so `add_contact_alias()` is dead code. This is proven by elimination from runtime evidence — the INVITE forwarded to Asterisk carries `Contact: <sip:ts-...@utcp-s2a-proof.invalid;transport=ws>` with **no** `;alias=` parameter, yet `result=websocket_contact_alias_failed` never fired and the INVITE completed with `200 OK`, leaving branch-not-entered as the only possibility. **`PRODUCT_DEFECT-13`:** `handle_ruri_alias()` returns success when the Request-URI carries no alias at all, so the committed `invalid_dialog_contact_alias` guard cannot detect the miss, `$du` stays empty and `t_relay()` falls back to ordinary DNS on the `.invalid` host — `sip_hostport2su(): could not resolve hostname` then `uri2dst2()` then `t_forward_nonack()` then `kamailio_application_dialog_relay_failed route=within_dialog method=BYE`, with Kamailio returning `478 Unresolvable destination (478/TM)` to Asterisk. That is the exact DNS fallback the contract forbids, and it masked `PRODUCT_DEFECT-12` by replacing the intended explicit failure with a generic resolution error. What the proof did establish: Asterisk generated a correct in-dialog BYE (matching Call-ID and tags, incremented CSeq, both route-set hops with the internal Kamailio Service first), it reached Kamailio, `has_totag()` routed it to `route[WITHINDLG]` and `loose_route()` succeeded with no initial-domain rejection and no repeated authentication; the browser never received the BYE, so the client returned no `200 OK` and the Asterisk channel terminated locally only. Regression boundaries hold: REGISTER is unchanged with no `;alias=` in the stored contact, client ACK still lands (0 post-ACK retransmissions) and the client-originated BYE still returns `200 OK` through the changed `WITHINDLG`, the `503` failure-route contract is intact, rtpengine stays uninvolved, both Kamailio Services remain ClusterIP-only, and the full-cluster Pod diff contains only the expected Kamailio rollout. Alias creation is correctly gated behind authentication and alias consumption behind `has_totag()` plus `loose_route()`, but the committed "invalid alias fails explicitly, never DNS fallback" property is not currently satisfied; the malformed-alias security case is untestable until `PRODUCT_DEFECT-12` is corrected. The next bounded slice is comparing `$proto` against the lowercase tokens and requiring `$du != ""` after `handle_ruri_alias()`, with matching static and mutation coverage, followed by a reproof of the last hop only. T3-S2A is In Progress, T3-S2 media mediation is Not Started, and T3 remains In Progress.
- T3-S2A repository correction (evidence `docs/evidence/t3/t3-s2a-websocket-alias-runtime-guard-correction.md`) records `PRODUCT_DEFECT-12 = corrected in repository` and `PRODUCT_DEFECT-13 = corrected in repository`: the authenticated initial application-dialog WebSocket guard now compares the lowercase live `$proto` values `ws` and `wss` only, preserving `add_contact_alias()` after authentication and before `record_route()`/Asterisk relay; `route[WITHINDLG]` now treats `handle_ruri_alias()` success as insufficient unless `$du` is non-empty afterward, returning explicit `400 Bad Request` for both `invalid_dialog_contact_alias` and `missing_dialog_contact_alias` before relay. Static validation and mutation coverage reject the dead uppercase guard, mixed-case compatibility guards, UDP aliasing, aliasing outside authentication, missing alias failure handling, empty-destination DNS fallback, fallback destinations, `$du` overwrites, REGISTER aliasing, stale checksums, and regressions in the existing ACK, client-BYE, Record-Route, unavailable-runtime `503`, DNS-policy, rtpengine, and public-surface contracts. No Kubernetes apply was performed. `PRODUCT_DEFECT-11 = ready for final live closure proof`. T3-S2A = ready for browser-bound BYE reproof. T3-S2 media mediation = Not Started. T3 = In Progress. **The T3-S2A WebSocket alias closure proof at `1381bf3` closes `PRODUCT_DEFECT-11`, `PRODUCT_DEFECT-12` and `PRODUCT_DEFECT-13`, but T3-S2A remains In Progress behind a new `PRODUCT_DEFECT-14`.** Exactly two resources were applied — the corrected `kamailio-config` ConfigMap (sha256 `3a38ad30...` to `2b92c60b...`, replacing the uppercase `"WS"/"WSS"` guard with lowercase `"ws"/"wss"` and adding the `missing_dialog_contact_alias` `$du` postcondition) and the checksum-coupled `kamailio` Deployment (generation 15 to 16) — producing a fully automatic ~4-second rollout to ReplicaSet revision 16 with no manual restart, zero ERROR lines, and running configuration byte-identical across repository render, live ConfigMap, in-Pod mount and Pod checksum annotation. A bounded packet capture in the Kamailio k3d node network namespace, filtered per Call-ID with Authorization redacted, proves the corridor end to end. **`PRODUCT_DEFECT-12` closed:** the lowercase guard executes and `add_contact_alias()` runs — zero uppercase comparisons remain. **`PRODUCT_DEFECT-11` closed:** the INVITE forwarded to Asterisk carries exactly one alias, `Contact: <sip:ts-...@utcp-s2a-proof.invalid;alias=10.42.0.150~36196~5;transport=ws>`, retaining the original browser identity and `.invalid` host; Asterisk retains the alias-bearing remote target, proven from the generated BYE Request-URI itself; that BYE reaches Kamailio, `has_totag()` routes it to `route[WITHINDLG]`, `loose_route()` succeeds, `handle_ruri_alias()` consumes the alias, `$du` becomes non-empty and the **existing** browser WebSocket connection is selected; the browser receives the BYE at `07:21:53Z` with matching Call-ID, tags and `CSeq: 5701 BYE`, answers `200 OK`, and that response was captured on the wire returning through Kamailio to Asterisk carrying the browser's own `User-Agent` — with `0` BYE retransmissions, no transaction timeout, no Kamailio-generated error, and the channel terminating with no manual cleanup. **`PRODUCT_DEFECT-13` closed:** one bounded synthetic in-dialog BYE with a distinct synthetic Call-ID and no alias returns `400 Bad Request` with `result=missing_dialog_contact_alias`, and one with a structurally invalid alias derived from the observed `ip~port~proto` format returns `400` with `result=invalid_dialog_contact_alias` (`nathelper: no proto in alias param`) — both with no DNS query, no `t_relay()`, no fallback and zero datagrams toward Asterisk. Across the whole proof there was no `sip_hostport2su()` failure, no `uri2dst2()` failure, no `t_forward_nonack()` failure and no `478 Unresolvable destination`. REGISTER is unchanged with no `;alias=` in registrar storage, the `503` failure-route contract is intact, rtpengine stays uninvolved, both Kamailio Services remain ClusterIP-only, and state authority is preserved. **`PRODUCT_DEFECT-14`:** the `$du` postcondition is applied to **every** in-dialog request, but only requests travelling toward the browser carry an alias. A request travelling toward Asterisk has a normal routable Request-URI (`sip:10.42.1.224:5060`), so `loose_route()` correctly leaves `$du` empty, `handle_ruri_alias()` finds no alias, and the guard wrongly rejects it `400 Bad Request` with `missing_dialog_contact_alias`. The ACK is therefore dropped (`post_ack_200_retransmissions` `0` to `3`, and Asterisk finally tore the channel down on its ACK timer, the source of the `Reason: cause=408` on the BYE) and the client-originated BYE returns `400 Bad Request` instead of `200 OK` — regressing two corridors that were `PASS` at `081267a` and `b547a98`. All static and parser checks pass because nothing asserts the postcondition applies only to alias-bearing Request-URIs. The smallest bounded correction is to guard the alias block on the presence of an `alias` URI parameter, e.g. `if ($du == "" && $(ru{uri.param,alias}) != "")`, with `config-check` and `config-check-test` coverage for both the unconditional-postcondition and dropped-postcondition mutations, followed by a reproof of ACK continuity, client-originated BYE and one clean CLI-triggered Asterisk-originated BYE only. T3-S2A is In Progress, T3-S2 media mediation is Not Started, and T3 remains In Progress.
- T3-S2A repository correction (evidence `docs/evidence/t3/t3-s2a-directional-dialog-alias-correction.md`) records `PRODUCT_DEFECT-14 = corrected in repository`: `route[WITHINDLG]` now consumes a WebSocket dialog alias only when the Request-URI contains an `alias` parameter, requires non-empty `$du` only for that alias-present browser-bound branch, explicitly rejects WS/WSS or `.invalid` browser targets with no alias using `result=missing_dialog_contact_alias`, preserves explicit malformed-alias failure using `result=invalid_dialog_contact_alias`, and allows alias-free Asterisk-bound ACK/BYE requests to relay on their routable Request-URI with `$du` empty. Static validation and mutation coverage now guard the full directional matrix while preserving the lowercase initial WS/WSS guard, one `add_contact_alias()`, Record-Route, Asterisk unavailable `503`, DNS-policy, REGISTER, rtpengine and public-surface contracts. No Kubernetes apply was performed. `PRODUCT_DEFECT-11` through `PRODUCT_DEFECT-14` = ready for consolidated live closure. T3-S2A = ready for final bidirectional dialog proof. T3-S2 media mediation = Not Started. T3 = In Progress. **The T3-S2A consolidated bidirectional dialog closure proof at `134a1d1` closes `PRODUCT_DEFECT-11` through `PRODUCT_DEFECT-14`; all five rows of the committed directional matrix pass and T3-S2A is Complete.** Exactly two resources were applied — the corrected `kamailio-config` ConfigMap (sha256 `2b92c60b...` to `e064cd33...`, replacing the unconditional `$du` postcondition with directional alias-present / browser-style / routable-target branches) and the checksum-coupled `kamailio` Deployment (generation 16 to 17) — producing a fully automatic ~5-second rollout to ReplicaSet revision 17 with no manual restart, zero ERROR lines, and running configuration byte-identical across repository render, live ConfigMap, in-Pod mount and Pod checksum annotation. Two bounded packet captures in the Kamailio k3d node network namespace, each filtered to one proof Call-ID with Authorization redacted, provide the wire evidence. **Row 1, browser ACK to Asterisk:** Request-URI `sip:10.42.2.150:5060`, alias absent, alias handling not invoked, and `post_ack_200_retransmissions = 0` — the decisive signal, which was `3` under the unconditional postcondition at `1381bf3`. **Row 2, browser BYE to Asterisk:** alias absent, empty `$du` accepted, relayed on the routable Request-URI, `200 OK` returned with matching Call-ID, tags and CSeq, dialog terminated, no alias failure log. **Row 3, Asterisk BYE to browser:** exactly one alias on the forwarded Contact (`;alias=10.42.0.164~48170~5`, ip~port~proto with proto 5 = WSS); a **clean CLI-triggered** hangup produced the BYE in the same second with **no `Reason: cause=408`**, proving the stimulus rather than an ACK timeout generated it; the alias was detected and consumed, `$du` became non-empty, the existing WebSocket connection was reused, the browser received the BYE and answered `200 OK`, and that response was captured returning through Kamailio to Asterisk carrying the browser's own `User-Agent` with `0` retransmissions, the Asterisk channel and browser dialog both terminating cleanly. **Row 4, missing browser alias:** `400 Bad Request` with `missing_dialog_contact_alias`. **Row 5, malformed browser alias:** `400 Bad Request` with `invalid_dialog_contact_alias` (`nathelper: no proto in alias param`). Both negative rows used distinct synthetic Call-IDs, never a live dialog identifier, and produced no DNS query, no `t_relay()`, no browser delivery, no Asterisk branch and no fallback — zero datagrams were captured for the synthetic Call-IDs. Across all five rows there was no `sip_hostport2su()` failure, no `uri2dst2()` failure, no `t_forward_nonack()` failure and no `478 Unresolvable destination`. REGISTER is unchanged with no `;alias=` in registrar storage, the `503` failure-route contract is intact in the running configuration, rtpengine sessions and ports_used remained `0` with zero operations, both Kamailio Services remain ClusterIP-only, no durable dialog authority appeared, and the full-cluster Pod diff contains exactly one change — the expected Kamailio rollout — with Asterisk, rtpengine and every unrelated workload retaining their UID and restart count. One environmental note: a host restart before the apply shuffled the k3d container IPs and k3s refused to start (`failed to find interface with specified node ip`); this was resolved with the established `k3d cluster stop`/`start` tooling only, restoring the original IP assignment with no repository change, no alternate kubeconfig and no cluster deletion, after which the baseline was re-recorded. CANCEL remains a bounded fixture property rather than a defect, because the `9900` fixture answers immediately. T3-S2A live signaling proof is Complete, T3-S2A is Complete, T3-S2 media mediation is Not Started, and T3 remains In Progress.
- T3-S2 repository correction (evidence `docs/evidence/t3/t3-s2-provider-neutral-media-mediation-correction.md`) adds provider-neutral rtpengine media mediation on top of the completed T3-S2A dialog route. Kamailio now invokes `MEDIA_OFFER`, `MEDIA_ANSWER`, and `MEDIA_DELETE` from SIP method, dialog direction, SDP presence and transaction lifecycle; rtpengine owns ephemeral RTP/RTCP/ICE relay state; and Asterisk remains the current reference application runtime rather than canonical media authority. Initial SDP INVITEs offer before `record_route()` and runtime relay, application-runtime SDP replies answer through a named reply route, in-dialog re-INVITE/UPDATE reuses the same generic media path, and BYE in both directions plus CANCEL/cancellation/terminal failure clean up through the single delete route. REGISTER remains outside media mediation, direct-media fallback and provider-specific Asterisk channel/ARI/AMI/dialplan authority are rejected by static and mutation coverage, and checksum coupling is updated. No Kubernetes apply was performed. T3-S2A = Complete. T3-S2 provider-neutral media mediation = corrected in repository and awaiting live proof. **The T3-S2 provider-neutral media live proof at `afced8d` is recorded in `docs/evidence/t3/t3-s2-provider-neutral-media-live-proof.md`: SDP-plane mediation and media-session lifecycle are fully proven, but actual RTP/SRTP media flow is not, so T3-S2 remains In Progress.** Exactly two resources were applied — the corrected `kamailio-config` ConfigMap (sha256 `e064cd33...` to `0d788689...`, adding `rtpengine.so`, the `rtpengine_sock` modparam pointed at the existing internal control Service `udp:rtpengine.utcp-platform.svc.cluster.local:2223`, and the generic `MEDIA_OFFER`, `MEDIA_ANSWER`, `MEDIA_DELETE` and `APPLICATION_RUNTIME_MEDIA_REPLY` routes) and the checksum-coupled `kamailio` Deployment (generation 17 to 18) — producing an automatic ~5-second rollout to ReplicaSet revision 18 with no manual restart, zero ERROR lines, running configuration byte-identical across all four authorities, and the rtpengine module binding its control endpoint at startup. Media authority is provider-neutral by evidence: scanned inside the media routes, ARI, AMI, Asterisk channel identifiers, Asterisk CLI, dialplan state, Pod-IP literals, database media state and Redis media state all occur **zero** times, there is no direct-media or `rtpproxy` fallback anywhere, and REGISTER carries zero media operations. Because no WebRTC-capable SIP client exists in the repository, a disposable one was driven inside the real application origin after a **natural first-party login** through the real login form (including the app's own forced password change), with the page obtaining its SIP credential from its own authenticated session and generating a genuine `RTCPeerConnection` offer (real ICE candidates, real DTLS fingerprint, `UDP/TLS/RTP/SAVPF`, `rtcp-mux`, deterministic 440 Hz Web Audio tone). Proven live: **MEDIA_OFFER** runs once per initial SDP INVITE, rtpengine accepts it, one session is created with ports allocated from the committed `40000-40099` range, and the runtime-facing offer is rewritten to `o=`/`c= 10.42.0.166` with `m=audio 40012 RTP/AVP` — ICE candidates, `ice-ufrag`, `fingerprint` and `setup` all stripped and **zero** browser addresses leaked; **MEDIA_ANSWER** runs once via `APPLICATION_RUNTIME_MEDIA_REPLY` and rewrites the runtime's own `c=IN IP4 10.42.2.150 / m=audio 12682 RTP/AVP` answer into `c=IN IP4 10.42.0.166 / m=audio 40092 RTP/SAVPF` with ICE, rtpengine's own DTLS fingerprint, `setup:passive`, `rtcp-mux` and PCMU — accepted by the real browser via `setRemoteDescription`, with the runtime Pod IP absent; **browser-originated BYE** logs `media_delete` with the matching Call-ID, returns `200 OK`, and returns sessions and ports to `0`; **runtime-originated BYE** from a clean CLI stimulus (no `Reason: cause=408`) is alias-routed to the browser, answered `200 OK` with `0` retransmissions, deletes the same media session and terminates both sides; **terminal runtime failure** deletes the created offer state from the failure route and emits the committed `asterisk_unavailable` `503` with no secondary runtime, no fallback and no residual session or port; and **rtpengine unavailability fails closed** with `media_offer_failed`, `no available proxies`, a committed `488 Media Relay Unavailable` to the client, and **no leak to the runtime** (`0 calls processed`). No direct-media path, Pod/node/developer-host address leak, public exposure or durable media authority appeared, and the full-cluster Pod diff contains only the Kamailio rollout plus the two intentional availability tests. **The unproven boundary is actual media flow:** the developer host has no route to the pod CIDR (`ip route get 10.42.0.166` resolves via the default gateway; a TCP probe times out), which is precisely the media containment T3-S1 proved, so a host-side browser cannot complete ICE/DTLS with rtpengine — the browser did receive and attempt rtpengine's candidate (`remoteCandidates ["10.42.0.166:40048"]`, ICE `checking`, DTLS `connecting`) but sent `0` packets. No host route was added, because that would breach a proven containment contract, so ICE completion, DTLS establishment, browser-to-runtime and runtime-to-browser SRTP and echo are reported unproven rather than inferred from SDP. CANCEL remains a bounded fixture limitation. The next step is an **in-cluster** WebRTC-capable media prover on the pod network to close the media-flow gap without weakening containment, after which the bounded FreeSWITCH parity gate applies. Asterisk remains the current reference runtime and runtime-agnostic parity is not yet proven. T3-S2 is In Progress and T3 remains In Progress. Asterisk = current reference runtime. Runtime-agnostic parity = not yet proven; a bounded second-runtime parity gate such as FreeSWITCH remains required after T3-S2 live proof. T3 = In Progress. `UTCP_PHASE=T1`.
- T3-S2B repository proof infrastructure (evidence `docs/evidence/t3/t3-s2b-in-cluster-webrtc-media-prover.md`) adds a local-only one-shot pinned Playwright/Chromium WebRTC prover that runs inside the Pod network, keeps canonical HTTPS/WSS origins, uses natural application login, reaches rtpengine media only through exact proof-only NetworkPolicies, emits structured RTP/audio evidence, and remains absent from production/base renders. T3-S2A bidirectional application-dialog signaling = Complete. T3-S2B in-cluster WebRTC media-flow proof = prover corrected in repository, live proof pending. T3-S2C second-runtime parity, preferably FreeSWITCH = Not Started. T3-S3 external media edge, advertised address, NAT/firewall and public reachability = Not Started. Asterisk = current reference runtime. Runtime-agnostic parity = not yet proven. External browser media readiness = not yet proven. T3 = In Progress. `UTCP_PHASE=T1`.
- **T3-S2B in-cluster WebRTC media live proof at `a39e6ba` (evidence `docs/evidence/t3/t3-s2b-in-cluster-webrtc-media-live-proof.md`) is INCOMPLETE: ICE, DTLS-SRTP and real outbound browser media are proven, inbound echo is blocked by `PRODUCT_DEFECT-15`, and six proof-harness defects are open in the committed prover.** Only the committed proof overlay was applied — `Namespace/utcp-proof`, `Job/utcp-proof/t3-media-prover`, the proof default-deny, the exact prover egress policy and the reciprocal `allow-rtpengine-media-prover` policy — with zero NodePort, LoadBalancer, ExternalIP, HostPort, hostNetwork, `ipBlock`, UDPRoute, Deployment or Service, media limited to UDP `40000-40099`, signaling to TCP `443` and DNS to UDP/TCP `53`; `kubectl diff -k` confirmed every proof resource was new. The prover ran from the pinned `mcr.microsoft.com/playwright:v1.61.1-noble` image (registry digest `sha256:bd90b3c1…`) as a one-shot non-root restricted Job, resolved the internal Traefik endpoint dynamically and preserved the canonical `https://app.utcp.local.test` and `wss://sip.utcp.local.test/ws` origins, host headers, SNI, cookie domain and certificate identity with no CoreDNS edit and no host route. Credentials were issued only through the canonical authorized API (`/api/v1/admin/users`, `/api/v1/admin/memberships`, natural member login with the app's own forced password change, `/api/v1/telephony/sessions`, then `signaling-credential` `201`, ~5 minute lifetime) and delivered through an external Secret; none reached a repository file, Job label, annotation, log or structured result. **Proven live:** the mediated answer delivered to a real Chromium `RTCPeerConnection` is exactly the committed contract — `m=audio 40068 RTP/SAVPF 0 126` and `c=IN IP4 10.42.0.179` on rtpengine's own address inside the committed `40000-40099` range, rtpengine's own DTLS fingerprint, `setup:passive`, `rtcp-mux`, rtpengine's own ICE credentials and host candidate, with no Asterisk, browser, public or developer-host address; ICE reached `connected` with a stable selected pair (local `10.42.1.241` prover Pod, remote `10.42.0.179` rtpengine media Pod, both private, no Asterisk media candidate); DTLS-SRTP completed with `peerConnectionState = connected` held for the full window; and real outbound SRTP flowed at ~50 pps for 12 s (`605` packets / `96 800` bytes, monotonic `packetsSent`/`bytesSent`) from the deterministic 440 Hz Web Audio source. **`PRODUCT_DEFECT-15`:** rtpengine cannot bridge RTP to the reference runtime. Asterisk answered, entered `from-kamailio` and ran `Echo()` for the full call, yet `pjsip show channelstats` reported `0` received and `0` transmitted RTP packets; pod interface counters over the same window read rtpengine `rx +645 / tx +625` against Asterisk `rx +22 / tx +25` (signaling only), and `rtpengine_one_way_sessions_total` incremented to `4` with `rtpengine_errors_total = 0`. A direct probe from the rtpengine Pod to the Asterisk Pod delivered `0` of 20 datagrams to both `19001` (Asterisk RTP range) and `40050` (rtpengine range), while the positive control — Kamailio to Asterisk on the permitted UDP `5060` — worked continuously (`8 calls processed`), proving cross-node Pod-to-Pod UDP is functional wherever a policy permits it; kube-router rebuilds its `KUBE-POD-FW-*` chains on resync so those packet counters were deliberately not used as evidence. The cause is a one-sided media plane: Asterisk's media ports are `rtpstart=10000`/`rtpend=20000`, `utcp-runtime` contains only `default-deny`, `allow-asterisk-sip-from-kamailio` (ingress UDP `5060`; egress UDP `5060` plus DNS, declared `policyTypes: [Ingress, Egress]`) and `allow-asterisk-ari-from-utcp-workers` (ingress TCP `8088`), so no policy permits media ingress to the runtime Pod and the runtime cannot send RTP back; and `allow-rtpengine-media` egress to `asterisk-ari` is scoped to UDP `40000-40099` — rtpengine's own range — while NetworkPolicy `ports` match the destination port, so rtpengine egress to `10000-20000` is denied at the source too. This is the same reciprocal-policy class as `docs/evidence/t3/t3-s1-rtpengine-reciprocal-egress-correction.md` and was never exercised before because `72716a3` explicitly recorded actual RTP flow as unproven. The smallest bounded correction is four exact rules — rtpengine egress to the runtime on UDP `10000-20000`, runtime ingress from rtpengine on UDP `10000-20000`, runtime egress to rtpengine on UDP `40000-40099`, and the already-correct rtpengine ingress from the runtime — with the runtime RTP range sourced from the same canonical place as `rtp.conf` and a `scripts/media/config-check` guard rejecting a media policy whose destination port range does not match the runtime's configured RTP range. **Six proof-harness defects:** `PROOF_HARNESS_DEFECT-1` the prover has no local-CA trust so natural login fails `net::ERR_CERT_AUTHORITY_INVALID` (no CA mounted, pinned image lacks `certutil`, `ignoreHTTPSErrors: false`, and the repository's `.runtime/tls/utcp-local-ca.crt` slot is unpopulated); `-2` the runner waits only on `condition=complete` for 480 s while the Job sets `ttlSecondsAfterFinished: 300`, so failed runs are garbage-collected before collection and both logs and the structured result are lost; `-3` `waitForMessage` returns `messages[cursor - 1]`, the message **before** the match, so the INVITE handler parses `100 trying` instead of the `200 OK` answer; `-4` `naturalLogin` waits only for `domcontentloaded` and queries selectors without auto-waiting, racing Vue SPA hydration; `-5` `bundlePolicy: 'max-bundle'` rejects rtpengine's answer because it carries no `a=group:BUNDLE`, while the default `balanced` policy accepts the identical answer; `-6` neither the runner nor `job.yaml` sets `UTCP_T3_MEDIA_PROVER_SCENARIO`, so Scenario B is unreachable through the committed entry point. Scenario B was correctly **not executed** — the contract requires all Scenario A media assertions to pass before the bounded runtime hangup stimulus, so no Asterisk CLI hangup was issued against any proof channel. No `PROOF_POLICY_DEFECT` was found; the proof-only policies carried the prover to rtpengine media, ICE and DTLS exactly as designed. Provider neutrality holds: the browser assertions depend only on SIP dialog state, ICE, DTLS/peer state, RTP statistics, audio energy, BYE direction and cleanup, and Asterisk channel identifiers, ARI, AMI, Pod IPs, dialplan context, Asterisk-specific SDP markers and FreeSWITCH ESL occur zero times in the prover; the only Asterisk-specific actions were read-only diagnostic CLI queries used to locate the defect, never as media authority. T3-S1 containment is fully preserved — no NodePort, LoadBalancer, ExternalIP, HostPort, HostNetwork, public UDPRoute, public rtpengine Service, k3d media publication, host route or Pod-CIDR `ipBlock`, and the external host still has no route to the internal rtpengine media candidate. A full-cluster Pod snapshot diff between baseline and final is **empty** — every workload retained its UID and restart count and all are Ready — with database tables `41`, tenants `27`, RuntimeNodes `110`, families unchanged, pending outbox `0`, Redis `sip`/`dialog`/`rtp`/`media` all `0`, and rtpengine sessions and `ports_used` back to `0` with `ports_free` `200`; Redis `db0` moved `1 → 34` from ordinary API session and cache entries, and nine proof users, memberships and telephony sessions were created through the canonical API only. All proof Jobs, Pods, Secrets, the proof ConfigMap, all three proof NetworkPolicies and the proof namespace were deleted, temporary pinned Helm `v4.0.3` was provisioned, verified and removed, `.playwright-mcp/` is absent, and no credential material remains. One environmental note: a host restart shuffled the k3d container IPs and k3s refused to start (`failed to find interface with specified node ip`), resolved with `k3d cluster stop`/`start` only, after which `scripts/security/check-apiserver-policy-drift` passed at `172.24.0.2/32:6443` and the baseline was recorded. T3-S2A bidirectional signaling = Complete. T3-S2B in-cluster WebRTC media proof = INCOMPLETE. T3-S2C second-runtime parity, preferably FreeSWITCH = Not Started. T3-S3 external media edge = Not Started. Contained in-cluster WebRTC media core = not proven. External browser media reachability = not proven. Asterisk = current reference runtime. Runtime agnosticism = not yet proven. T3-S2 overall = In Progress. T3 = In Progress. `UTCP_PHASE=T1`.
- **T3-S2B reciprocal media corridor and prover reproof at `48dc81e` (evidence `docs/evidence/t3/t3-s2b-reciprocal-media-corridor-live-proof.md`) CLOSES `PRODUCT_DEFECT-15` but remains INCOMPLETE: the media contract is proven end to end, while the committed prover still cannot execute and one new `PRODUCT_DEFECT-16` is open.** Only the two production NetworkPolicies changed by `fde07d5` were applied — `allow-rtpengine-media` (`utcp-platform`, generation `1 → 2`) and `allow-asterisk-sip-from-kamailio` (`utcp-runtime`, generation `3 → 4`) — and `kubectl diff` for each contained exactly the four required directions (rtpengine egress → runtime UDP `10000-20000`, runtime ingress ← rtpengine UDP `10000-20000`, runtime egress → rtpengine UDP `40000-40099`, rtpengine ingress ← runtime UDP `40000-40099`) with existing SIP `5060`, DNS `53`, control `2223` and metrics `2224` rules intact, namespace-plus-Pod selectors, no `ipBlock`, no all-UDP rule and no public exposure; the secondary runtime is excluded because both the peer selector and the policy's own `podSelector` require `utcp.dev/runtime-node: local-asterisk-ari`. NetworkPolicy application restarted **no** Pod (post-apply full-cluster Pod diff identical). A corridor probe proved the rule is open **and** exactly scoped: rtpengine → runtime `19001` (inside the range) delivered `20/20` where it previously delivered `0`, while `40050` (outside) remained denied `0/20`. **`fde07d5` also required a deployment step the policies alone do not provide:** it adds `infrastructure/docker/asterisk/config/rtp.conf`, which is baked into the Asterisk image and staged by the entrypoint into the effective configuration directory `/tmp/utcp-asterisk` (Asterisk runs `-C /tmp/utcp-asterisk/asterisk.conf`, never `/etc/asterisk`). The running Pod predated the commit, staged no `rtp.conf`, and used the compiled-in defaults `Port start 5000 / Port end 31000`, so only calls whose runtime RTP port happened to land inside the permitted `10000-20000` window succeeded — a node sniff captured the mismatch directly (`10.42.0.179:40004 -> 10.42.2.170:29938`, outside the range). The committed Asterisk image was rebuilt from HEAD and only `deploy/asterisk-ari` rolled out, after which `rtp show settings` reports `10000-20000`; this is a divergence from the prompt's "expected production workload restarts: none", which assumed the commit touched NetworkPolicies alone. **Media proven end to end:** rtpengine's own end-of-session accounting shows both legs symmetric — browser leg `10.42.0.179:40044 <> 10.42.1.2:50420` `in 2138 p / out 2130 p` over `RTP/SAVPF` with `DTLS-SRTP … AEAD_AES_256_GCM`, runtime leg `10.42.0.179:40078 <> 10.42.1.254:19948` `in 2120 p / out 2120 p` over `RTP/AVP` plus RTCP on `:19949`, average MOS `4.3`, packet loss `0/0/0%`; Asterisk reported `rx 680 / tx 680` while `Echo()` remained the running application; and the browser received a deterministic echo of its 440 Hz tone with inbound exactly matching outbound (`605/96800` both directions), `inbound-rtp.totalAudioEnergy` climbing monotonically to `0.3864`, jitter `0.002` and `0` packets lost. **Scenario A** (explicitly selected `browser-originated-bye`) reached `Complete=True`, pod `Succeeded`, exit `0`, ICE `connected` on a stable pair whose remote candidate is the rtpengine media Pod (host/private, no Asterisk media candidate), DTLS `connected`, browser BYE answered `SIP/2.0 200 OK`, media session deleted and ports released. **Scenario B** (explicitly selected `runtime-originated-bye`) reached `Complete=True`, exit `0`: the bounded Asterisk CLI hangup was issued only after the prover's committed readiness marker plus independent runtime-side evidence (`channelstats rx 668 / tx 668`, `active_channels=1` asserted against a `0` baseline, extension `9900`, `Up`, `Echo`), used `channel request hangup` on exactly that one channel with no ARI, AMI, dialplan change or broad termination and no `Reason: cause=408`, and the resulting BYE reached the browser over the existing WebSocket through the proven alias corridor, was answered `200 OK`, terminated both sides and released the media session with no retransmission or timeout. Evidence was collected before cleanup in every run; the corrected runner waits for `Complete=True` **or** `Failed=True` and `ttlSecondsAfterFinished` is `900`, closing the previous proof's collection defect, and scenario selection is now explicit and validated before Job creation, closing the previous scenario defect. **Six proof-harness defects remain open in the committed prover, so every media result above was obtained with a scratchpad diagnostic prover differing only by those six corrections while the committed namespace, Job and all three committed proof NetworkPolicies were applied unchanged:** `A` `certutil` is absent because `tools/t3-media-prover/Dockerfile` never installs `libnss3-tools` (`spawn certutil ENOENT`, exit `1` in 13 ms); `B` `--user-data-dir` is passed to `chromium.launch()` and Playwright rejects it, demanding `launchPersistentContext`; `C` the NSS database is built in a private profile directory while Chromium reads trust only from `$HOME/.pki/nssdb`; `D` login still races SPA hydration because `#app` is visible before the form exists and `firstLocator()` uses non-auto-waiting `count()`; `E` audio energy is read from the removed `track` statistic, which is `0` in every run, instead of `inbound-rtp.totalAudioEnergy`; `F` in-dialog ACK and BYE reuse the original request URI instead of the dialog remote target from the 200 OK `Contact`, so the ACK never lands — the exact T3-S2A signature of three `200 OK` retransmissions followed by a runtime BYE and `408 Request Timeout`, fully resolved by targeting `sip:10.42.1.254:5060` (post-ACK retransmissions `0`). With the CA delivered to `$HOME/.pki/nssdb`, natural login succeeded under **normal TLS verification** — no `ignoreHTTPSErrors`, no `--ignore-certificate-errors`, no insecure origin, no ClusterIP browser URL and no private key in the Pod. **`PRODUCT_DEFECT-16`:** rtpengine terminated twice with `SIGSEGV` (`exitCode 139`, restart count `0 → 2`, Pod UID unchanged) immediately after the per-call `Final packet stats` block, i.e. during media-session teardown; both crashes fall inside the pre-correction window when the corridor was half-open, media was one-way and sessions were abandoned without a BYE, and it did not recur across the four clean cycles that followed — it needs its own bounded diagnosis. No `PROOF_POLICY_DEFECT` was found. One `PROOF_LIMITATION`: Asterisk 20's `pjsip show channel` does not expose the SIP Call-ID, so CLI-side literal matching is unavailable and correlation rested on a `0` baseline, exactly one asserted active channel, extension `9900`, matching media counters and — decisively — the browser matching the inbound BYE against its own Call-ID before answering. Provider neutrality holds: the prover asserts only on SIP dialog state, ICE, DTLS/peer state, RTP packets and bytes, audio energy, BYE direction, final SIP response and cleanup, and `t3-media-prover-config-check` enforces the absence of ARI, AMI, Asterisk channel identifiers, `fs_cli`, FreeSWITCH ESL, `hostNetwork`, `ignoreHTTPSErrors`, `--ignore-certificate-errors` and `max-bundle`; the Asterisk CLI appears only as the Scenario B stimulus and read-only diagnosis. T3-S1 containment and default-deny are fully preserved — no NodePort, LoadBalancer, ExternalIP, HostPort, HostNetwork, public UDPRoute, public rtpengine Service, k3d media publication, host route, Pod-CIDR `ipBlock` or direct prover-to-Asterisk media permission — and the external host still has no route to the internal rtpengine media candidate. State authority is unchanged (tables `41`, tenants `27`, RuntimeNodes `110`, families unchanged, pending outbox `0`, Redis `sip`/`dialog`/`rtp`/`media` `0/0/0/0`, rtpengine sessions `0/0`, `ports_used 0` / `ports_free 200`, Asterisk `0` active channels); Redis `db0` moved `34 → 74` from ordinary API session and cache entries. The full-cluster Pod diff contains exactly two entries — the rtpengine restart count and the intentional Asterisk rollout — with Kamailio, the secondary runtime `asterisk-ari-b` and every unrelated workload retaining UID and restart count. All proof Jobs, Pods, Secrets, ConfigMaps, NetworkPolicies, the proof namespace and the diagnostic images were removed; temporary pinned Helm `v4.0.3` was provisioned, verified and removed; `.playwright-mcp/` is absent; the corrected production media NetworkPolicies and the deployed Asterisk image remain in place. The smallest bounded correction is one prover slice: install `libnss3-tools`, build the NSS database at `$HOME/.pki/nssdb` and drop `--user-data-dir`, read `inbound-rtp.totalAudioEnergy`, use the dialog remote target for in-dialog ACK/BYE, replace the `#app` wait with an auto-waiting email locator, and extend `t3-media-prover-config-check` plus its mutation coverage to assert all four. `PRODUCT_DEFECT-15` = closed. `PRODUCT_DEFECT-16` = open. T3-S2B in-cluster WebRTC media proof = INCOMPLETE. T3-S2C second-runtime parity = Not Started. T3-S3 external media edge = Not Started. Contained in-cluster media core = proven. External browser media reachability = not proven. Asterisk = current reference runtime. Runtime agnosticism = not yet proven. T3-S2 overall = In Progress. T3 = In Progress. `UTCP_PHASE=T1`.
- **T3-S2B committed-prover media reproof at `708f196` (evidence `docs/evidence/t3/t3-s2b-committed-prover-build-blocker.md`) is INCOMPLETE and blocked at image build by `PROOF_HARNESS_DEFECT-G`: neither scenario ran.** `tools/t3-media-prover/Dockerfile` verifies its `libnss3-tools` install with `certutil -H`, which exits `1` by design because it prints usage, so the `RUN` layer can never succeed and `make image-build-t3-media-prover` aborts (`Setting up libnss3-tools (2:3.98-1ubuntu0.2)` then `exit code: 1`). Measured directly in the pinned base image after installing the package: `certutil` resolves to `/usr/bin/certutil`, `certutil -H` exits `1`, `certutil -V` exits `255`, `certutil --version` exits `1`, while `certutil -N -d sql:/tmp/nss --empty-password` exits `0` — the package and binary are fully functional and only the verification command is wrong. The smallest bounded correction is one line: replace `certutil -H >/dev/null 2>&1` with a presence check that exits zero on success (`command -v certutil >/dev/null`, optionally plus a throwaway `certutil -N`), and add a `scripts/t3-media-prover/config-check` guard with mutation coverage rejecting a verification command that exits non-zero on success. Because no image could be produced, **no proof namespace, Job, Secret, ConfigMap, or proof NetworkPolicy was applied**, no production resource was changed, and the full-cluster Pod snapshot diff is empty — every workload retained its UID and restart count and all remained Ready. The committed runner was deliberately **not** executed: the local registry still held a `utcp/t3-media-prover:0.1.0-k1-dev` tag built from pre-`708f196` source, so a run would have silently executed the old prover and produced a result not representing the committed implementation; that tag and all three node-cached copies were removed. **Everything verifiable without the image passes.** Source review confirms prover defects `B`–`F` are correctly addressed: `--user-data-dir` is gone from `chromium.launch()`; the NSS database is built at `$HOME/.pki/nssdb` and recreated per run; the login race is closed by auto-waiting `waitFor` plus `waitForLocatorReady` with `firstLocator`/`fillFirst`/`clickFirst` deleted; audio energy now comes from `inbound-rtp.totalAudioEnergy` with the `track` branch removed and a new `audioEnergySource` field asserted in both `assertMediaCounters` and `assertStructuredResult`; and `createDialog()` stores the 2xx `Contact` as `remoteTarget` so ACK and BYE use it with a distinct `To` URI, while `waitForMessage` now returns the matched message and advances the shared cursor — covered by the new `tools/t3-media-prover/sip-dialog.mjs` plus `sip-dialog-test.mjs` (`node tools/t3-media-prover/sip-dialog-test.mjs` passes) and the extended `config-check` mutation tests. `prover.mjs` does not import `sip-dialog.mjs` (its browser-context copy is inlined), so the Dockerfile `COPY` set is correct. The rendered proof overlay contains only the committed proof resources with exactly DNS UDP/TCP `53`, application/WSS TCP `443` and rtpengine media UDP `40000-40099`, no Deployment, Service, NodePort, LoadBalancer, ExternalIP, HostPort, HostNetwork, `ipBlock`, UDPRoute or hostPath, no direct prover-to-Asterisk media permission, and correct public-CA-only trust wiring (`NODE_EXTRA_CA_CERTS=/etc/ssl/certs/utcp-local-ca.crt`, `local-ca` volume from ConfigMap `t3-media-prover-local-ca` key `ca.crt` mounted read-only via `subPath`, scenario from `configMapKeyRef`, credentials via `secretRef`, no private key); the prover source contains no `ignoreHTTPSErrors`, `--ignore-certificate-errors`, `--user-data-dir` or `max-bundle`. An **independent Playwright MCP natural login proof** started at the real login page over HTTPS with no injected cookie, local-storage, Redis or database state, submitted the real form, and reached `https://app.utcp.local.test/dashboard`; `GET /api/v1/auth/session` returned `200` authenticated with `password_change_required false`, one membership to tenant slug `local` ("Local Tenant", `7be59d2a-…`, membership and tenant status `active`) and an empty capability set for a plain tenant-member, then logout returned `200` and the session subsequently returned `401`. That proof directly validates the `708f196` login changes: `input[name="email"]` does **not** exist on the page — the source of the earlier hydration race — while `input[type="email"]`, `input[type="password"]`, `button[type="submit"]` (text "Sign in") and the post-login `.app-shell, nav, [data-testid="app-shell"]` wait all resolve (2 matches: `.app-shell` and `nav`). Its session was not passed to anything else and `.playwright-mcp/` was removed. **`PRODUCT_DEFECT-16` remains open and unconfirmed with no new evidence**: rtpengine kept uid `0fbd6b20-…`, restart count `2`, container start `2026-08-01T22:17:53Z` and its historical `exitCode 139` last-termination record unchanged, with sessions `0/0` and `ports_free 200`; no root cause is claimed. State authority is unchanged (tables `41`, tenants `27`, RuntimeNodes `110`, families unchanged, pending outbox `0`, Redis `sip`/`dialog`/`rtp`/`media` `0/0/0/0`, Asterisk `0` active channels, `rtp show settings` still `10000-20000`, production media NetworkPolicy generations still `2` and `4`). One proof member, membership and telephony session were created through the canonical API for the MCP login proof only. Temporary pinned Helm `v4.0.3` was provisioned, verified and removed; no diagnostic image, scratchpad prover copy, port-forward or credential material remains. One `PROOF_LIMITATION` is recorded for the next run: the committed Scenario B readiness marker `document.body.dataset.utcpMediaReady` is in-page only and emits no log line, so an external controller cannot observe it directly and must gate the hangup on an equivalent observable condition; emitting a readiness line to stdout would make the committed contract directly observable. `PRODUCT_DEFECT-15` = closed (unchanged). `PROOF_HARNESS_DEFECT-G` = open. T3-S2B in-cluster WebRTC media proof = INCOMPLETE. T3-S2C second-runtime parity = Not Started. T3-S3 external media edge = Not Started. Contained in-cluster media core = not proven with the committed prover. External browser media reachability = not proven. Asterisk = current reference runtime. Runtime agnosticism = not yet proven. T3-S2 overall = In Progress. T3 = In Progress. `UTCP_PHASE=T1`.
- **T3-S2B committed-image WebRTC media proof at `5bc95aa` (evidence `docs/evidence/t3/t3-s2b-committed-image-media-live-proof.md`) is COMPLETE: both committed scenarios pass with the committed image, runner and manifests, and rtpengine did not restart.** `make image-build-t3-media-prover` now succeeds — the `5bc95aa` verification replaces the always-failing `certutil -H` with an executable-presence check plus a temporary SQL NSS database (`certutil -N`, `cert9.db`/`key4.db` assertions, directory removal), closing **`PROOF_HARNESS_DEFECT-G`**. Image identity verified inside the build (`uid=1000(ubuntu) gid=1000`, `HOME=/home/ubuntu`, `certutil=/usr/bin/certutil`, node `v24.17.0`, no residual NSS databases anywhere, `/home/ubuntu/.pki` absent until runtime) and published as `utcp-local-registry:5000/utcp/t3-media-prover:0.1.0-k1-dev` digest `sha256:e22fce960525a8e3249ac0cb8d2bbc74df6c2eb5482167feef6234fadd2a9304` from source `5bc95aa`; the live Scenario A Pod `t3-media-prover-lf2vb` reported `imageID …@sha256:e22fce960525…`, i.e. **digest equality, not tag equality**, and the node inventory confirms only that digest exists for the tag. Trust operated under **normal TLS verification** — the prover created `$HOME/.pki/nssdb`, imported the public local CA as `UTCP local public CA`, and the source contains no `ignoreHTTPSErrors`, `--ignore-certificate-errors` or `--user-data-dir`, with no private-key mount, no ClusterIP browser URL, no CoreDNS edit and no host route. An independent **Playwright MCP natural login** from the real HTTPS login page (waiting through pre-hydration `title "web"` to `"Sign in - UTCP"`) filled the real email/password controls, submitted the real form, reached `/dashboard` with the app shell present, returned `HTTP 200` from `/api/v1/auth/session` (`user.status active`, `password_change_required false`, one membership to tenant slug `local` "Local Tenant" both statuses `active`, empty capability set, `catalog_version c5.2026-07-15`), then logged out `200` with the session subsequently `401`; its session was never passed to the in-cluster prover and `.playwright-mcp/` was removed. **Scenario A** (`browser-originated-bye`, explicitly selected, committed runner) exited `0` with `job_terminal_state=complete pod_phase=Succeeded`, `errors []`, Call-ID `0c7fa91907c618@utcp-t3-media-prover`: ICE gathering `complete` with a selected pair whose local and remote candidates are both `host`/private (prover Pod ↔ rtpengine media Pod, no Asterisk media candidate), DTLS `connected`, outbound `179 p / 28640 b` exactly equal to inbound `179 p / 28640 b`, `audioEnergy 0.11235139465764898` sourced from `inbound-rtp.totalAudioEnergy`, jitter `0.002`, `packetsLost 0`; rtpengine per-leg accounting for that Call-ID shows the browser leg `10.42.0.179:40030 <> 10.42.1.6:53403` (`RTP/SAVPF`, in 187 p / out 181 p) and the runtime leg `10.42.0.179:40002 <> 10.42.1.254:15094` (`RTP/AVP`, in 179 p / out 185 p) plus RTCP on `:15095`, with the runtime destination inside the `10000-20000` range and the corrected policy window; Asterisk reported symmetric `rx 164 / tx 164` under `Echo()`. The committed `createDialog()` stored the 2xx `Contact` as the dialog remote target and ACK/BYE used it — proven live by **zero `200 OK` retransmissions and no `Reason: cause=408`** — and the browser BYE received `SIP/2.0 200 OK`, after which the media session disappeared and ports returned to `0/200` with the Asterisk channel terminated. **Scenario B** (`runtime-originated-bye`, explicitly selected) exited `0` with `errors []`, Call-ID `6d68b6127ddd5@utcp-t3-media-prover`: the committed stdout marker `UTCP_T3_MEDIA_PROVER_READY_FOR_RUNTIME_HANGUP` was observed **exactly once**, only in Scenario B (zero occurrences in Scenario A), after all media assertions, while the Job was still active and carrying no sensitive content; the bounded Asterisk CLI stimulus then targeted exactly one channel selected against a zero-channel baseline (`active_channels=1`, `PJSIP/anonymous-00000005`, exten `9900`, state `Up`, `channelstats rx 215 / tx 215`) using `channel request hangup` only — no ARI, AMI, dialplan change, reload or broad termination, and with reciprocal RTP already positive so the ACK had landed with no retransmitted `200 OK` and no `408`. The runtime BYE then reached the browser over the existing WebSocket, the browser matched it to its own dialog and returned `200 OK`, `MEDIA_DELETE` executed, the rtpengine session disappeared, ports returned to zero and both sides terminated with no retransmission or timeout; Scenario B media matched Scenario A (`179/28640` both directions, `audioEnergy 0.11235139465764898` from `inbound-rtp.totalAudioEnergy`, jitter `0.002`, loss `0`) with per-leg accounting `10.42.0.179:40088 <> 10.42.1.7:60472` (SAVPF, in 226 / out 222) and `10.42.0.179:40062 <> 10.42.1.254:16728` (AVP, in 220 / out 224) plus RTCP on `:16729`. Structured results, Job conditions, Pod phases and exit codes were collected **before** cleanup in both runs. **`PRODUCT_DEFECT-16` was not reproduced**: the rtpengine Pod UID `0fbd6b20-…`, restart count `2`, container start `2026-08-01T22:17:53Z` and its historical `exitCode 139 / finishedAt 22:17:40Z` last-termination record were identical before Scenario A, between the scenarios and after Scenario B, with `Ready=True` and **zero** `ERROR`/`CRIT`/`SEGV` lines in the current container — it does not block T3-S2B closure or FreeSWITCH parity, and no root cause is claimed and no workaround introduced. Provider neutrality holds: success criteria use only SIP dialog state, the response `Contact` remote target, the route set, ICE, DTLS, RTP packets and bytes, `inbound-rtp.totalAudioEnergy`, BYE direction, final SIP response and cleanup state, with `t3-media-prover-config-check` rejecting ARI, AMI, Asterisk channel identifiers, `fs_cli`, FreeSWITCH ESL, `hostNetwork`, `ignoreHTTPSErrors`, `--ignore-certificate-errors` and `max-bundle`; the Asterisk CLI appeared only as the Scenario B stimulus. Containment is fully preserved — rtpengine and `asterisk-sip` Services remain ClusterIP-only, the only LoadBalancer is Traefik on TCP `80/443`, there is no k3d UDP/media publication, HostPort, HostNetwork, public UDPRoute, Pod-CIDR `ipBlock` or direct prover-to-Asterisk media permission, proof egress stayed exactly DNS UDP/TCP `53` + WSS TCP `443` + media UDP `40000-40099`, and `ip route get 10.42.0.179` still resolves via the default gateway so the external host has no route to the media candidate. The full-cluster Pod snapshot diff is **empty** — every workload retained UID and restart count and all remained Ready — with tables `41`, tenants `27`, RuntimeNodes `110`, families unchanged, pending outbox `0`, Redis `sip`/`dialog`/`rtp`/`media` `0/0/0/0`, rtpengine sessions `0/0`, `ports_used 0` / `ports_free 200`, Asterisk `0` active channels, and production media NetworkPolicy generations unchanged at `2` and `4`. All proof Jobs, Pods, Secrets, scenario and public-CA ConfigMaps, proof-only NetworkPolicies and the proof namespace were removed by the committed runner; the structured-result scratch directory, temporary pinned Helm `v4.0.3` and `.playwright-mcp/` were removed; no credential material remains. One `PROOF_LIMITATION` stands: Asterisk 20's CLI does not expose a channel's SIP Call-ID, so channel selection used a zero-channel baseline plus an asserted single `9900`/`Up` channel with matching media counters — dialog identity is nonetheless exact because the prover matched the inbound BYE against its own Call-ID before answering. `PRODUCT_DEFECT-15` = closed. `PROOF_HARNESS_DEFECT-G` = closed. Committed prover defects A-F = closed. T3-S2B in-cluster WebRTC media proof = **Complete**. T3-S2C second-runtime parity = Not Started. T3-S3 external media edge = Not Started. Contained in-cluster media core = proven using the committed image and scripts. External browser media reachability = not proven. Asterisk = current reference runtime. Runtime agnosticism = not yet proven. T3-S2 overall = In Progress pending second-runtime parity. T3 = In Progress. `UTCP_PHASE=T1`.
- **T3-S2C FreeSWITCH live parity proof at `e294ac7` (evidence `docs/evidence/t3/t3-s2c-freeswitch-parity-runtime-blocker.md`) is INCOMPLETE: the committed image builds but FreeSWITCH cannot start, so no parity scenario ran and the overlay was deliberately not applied.** Every committed static check passes (`freeswitch-config-check`/`-test`, media, Kamailio signaling, security, prover) because none of them executes the entrypoint or parses the FreeSWITCH XML — the defects are runtime-only. **`PRODUCT_DEFECT-17`:** `infrastructure/docker/freeswitch/entrypoint` runs `freeswitch -nonat -nf -c /etc/freeswitch/freeswitch.xml`, but FreeSWITCH's `-c` means "output to a console and stay in the foreground" and takes **no argument** (the config *directory* flag is `-conf`), so the path becomes a stray option and the process exits `1` with `Unknown option '/etc/freeswitch/freeswitch.xml'`; the correction must also supply `-log` and `-db` because FreeSWITCH rejects `-conf` alone with `You must specify all or none of -conf, -log, and -db`. Failed seam: SIP listener. **`PRODUCT_DEFECT-18`:** with the flag corrected the committed `freeswitch.xml` fails to parse — `Cannot Initialize [[error near line 7]: markup outside of root element]` — because it uses `<include>` with invented `<settings>`, `<modules>`, `<profiles>` and `<dialplans>` top-level elements instead of the FreeSWITCH schema `<document type="freeswitch/xml">` with `<section name="configuration">`/`<section name="dialplan">` children (mod_sofia reads profiles from `sofia.conf.xml`, not a `<profiles>` list), as confirmed against the base image's own `/usr/share/freeswitch/conf/vanilla/freeswitch.xml`; consequently no module loads, **no socket ever binds**, the `9900` echo fixture is never registered and the `21000-21099` RTP range is never applied. Failed seams: SIP listener, and consequently SDP offer, SDP answer, RTP range and Echo. **`PRODUCT_DEFECT-19`** (latent behind the first two): the Dockerfile sets `USER 1000:1000` but `/var/lib/freeswitch` is owned `995:0` mode `755` and `/var/log/freeswitch` and `/var/run/freeswitch` do not exist in the image, while the Deployment mounts `emptyDir` at all three with `runAsUser/runAsGroup 1000`, `readOnlyRootFilesystem: true` and **no `fsGroup`**, so those volumes are created `root:root` and are unwritable by the runtime uid. Failed seam: SIP listener. The image itself built cleanly from the pinned base `safarov/freeswitch:1.10.12@sha256:b31c743f…` as local ID `3e8c169eb3a0` (FreeSWITCH `1.10.12-release`, uid/gid `1000:1000`), but was **never published or deployed** and its local tag was removed at cleanup. **The overlay was deliberately not applied**: with FreeSWITCH unable to start, applying it would have scaled `asterisk-ari` to `0`, rolled Kamailio onto the new ConfigMap whose `APPLICATION_RUNTIME_RELAY` targets `application-runtime-sip.utcp-runtime.svc.cluster.local:5060`, and pointed that Service exclusively at crash-looping FreeSWITCH Pods — leaving the local environment with zero Ready application-runtime endpoints and no working telephony runtime, for no proof value, when the blocker is already proven deterministically and more cheaply at the image level. Static overlay review nonetheless records the design as correctly shaped for the retry: a single selected-runtime `application-runtime-sip` ClusterIP Service narrowed by the overlay to `component=freeswitch` + `runtime-node=local-freeswitch-esl` + `runtime-selection=selected-application-runtime`, Asterisk patched to `replicas: 0`, and `allow-freeswitch-sip-from-kamailio` carrying properly reciprocal rules (ingress UDP `5060` from kamailio-signaling and UDP `21000-21099` from rtpengine-media; egress UDP `5060` to kamailio-signaling, UDP `40000-40099` to rtpengine-media, and UDP/TCP `53` to kube-dns), with no public SIP Service, ESL Service, public media, NodePort, LoadBalancer, HostPort, HostNetwork, `ipBlock`, all-UDP or all-egress rule, dual runtime selector, Asterisk fallback selector, Kamailio media-route duplication or browser-prover modification; Kamailio's rename to the generic `APPLICATION_RUNTIME_RELAY`/`APPLICATION_RUNTIME_UNAVAILABLE` routes is in place with no FreeSWITCH-specific duplicate route. Consequently **not executed**: FreeSWITCH startup/configuration verification, runtime-selection projection, generic Service endpoints, Asterisk scale-to-zero, Playwright MCP natural login, Scenario A, Scenario B, the readiness marker, the bounded hangup stimulus, selected-runtime-unavailable behavior, Asterisk zero-fallback counters and default-overlay restoration. No proof namespace, Job, Secret, ConfigMap or NetworkPolicy was created and `.playwright-mcp/` was never created. **Production is untouched**: the full-cluster Pod snapshot diff is empty with every UID and restart count preserved and all workloads Ready, `application-runtime-sip` is still absent, no FreeSWITCH resource or policy exists, the live Kamailio ConfigMap still carries `ASTERISK_RELAY -> asterisk-sip`, `asterisk-ari` remains `replicas 1 ready 1` and selected, tables `41`, tenants `27`, RuntimeNodes `110` (asterisk, simulator), pending outbox `0`, Redis `sip`/`dialog`/`rtp`/`media` `0/0/0/0`, rtpengine sessions `0/0` with `ports_used 0`, Asterisk `0` active channels, and all production NetworkPolicy generations unchanged. One `PROOF_LIMITATION` is recorded: `freeswitch-config-check` validates file presence and manifest shape only, so a build-time startup smoke test asserting a bound SIP socket is the guard that would catch this entire class. Also flagged for the retry: the Deployment's readiness and liveness probes use `tcpSocket` against the container port named `sip`, which is declared `UDP` — unverifiable here because nothing ever listened. The smallest bounded correction is four changes: fix the entrypoint flags (`-c -conf <dir> -log <dir> -db <dir> -run <dir>`), rewrite `freeswitch.xml` to the `<document type="freeswitch/xml">` schema with `modules.conf`/`sofia.conf` and the `9900` dialplan, add `fsGroup: 1000` with matching writable paths, and extend `freeswitch-config-check` with guards for all three plus a startup smoke test. T3-S2A = Complete. T3-S2B = Complete. T3-S2C FreeSWITCH parity = INCOMPLETE. `PRODUCT_DEFECT-17`, `PRODUCT_DEFECT-18` and `PRODUCT_DEFECT-19` = open. Provider-neutral signaling and media contract = proven against Asterisk only. Runtime agnosticism = not yet proven. T3-S2 overall = In Progress. T3-S3 external media edge = Not Started. T3 = In Progress. `UTCP_PHASE=T1`.
- **T3-S2C corrected FreeSWITCH parity proof at `0f3b168` (evidence `docs/evidence/t3/t3-s2c-freeswitch-parity-overlay-blocker.md`) is INCOMPLETE: the runtime corrections are good and `PRODUCT_DEFECT-17`, `-18`, `-19` and `-20` are closed, but the proof is blocked one step later by a new `PRODUCT_DEFECT-21` in the parity overlay, so no scenario ran and nothing was applied.** The image builds and publishes from `0f3b168` as `utcp-local-registry:5000/utcp/freeswitch:0.1.0-t3-s2c` digest `sha256:e46205af189886b33a81d02c47b849d73fb79a2a0f8c641685f7316cccc4645d` (base `safarov/freeswitch:1.10.12@sha256:b31c743f…`, uid/gid `1000:1000`, configuration digest `1009fddac88323b0…`), and `make freeswitch-startup-smoke-test` runs the real image under the **exact committed entrypoint** and observes `utcp-internal profile sip:mod_sofia@…:5060 RUNNING (0)`. `PRODUCT_DEFECT-17` closed: the entrypoint now uses `freeswitch -nonat -c -conf /etc/freeswitch -log /var/log/freeswitch -db /var/lib/freeswitch/db -run /var/run/freeswitch`, supplying `-conf`, `-log` and `-db` together. `PRODUCT_DEFECT-18` closed: `freeswitch.xml` is the official `<document type="freeswitch/xml">` schema with `configuration`/`dialplan`/`chatplan`/`directory`/`languages` sections, `autoload_configs/` supplying `modules.conf`, `switch.conf` (`rtp-start-port 21000`/`rtp-end-port 21099`), `sofia.conf` and `event_socket.conf`, the profile at `sip_profiles/utcp-internal.xml` and the `9900` fixture at `dialplan/utcp.xml`; the Dockerfile also asserts every required module `.so` exists at build time. `PRODUCT_DEFECT-19` closed: the entrypoint creates and asserts writability of `/var/log/freeswitch`, `/var/run/freeswitch` and `/var/lib/freeswitch/db`, and the Deployment adds `fsGroup: 1000`. `PRODUCT_DEFECT-20` closed at image level: the `tcpSocket` probe against a UDP-declared port is replaced by the executable `/usr/local/bin/utcp-freeswitch-healthcheck` (startup, readiness and liveness) asserting `status` `UP` and `utcp-internal … RUNNING` over loopback `fs_cli` — unverified inside Kubernetes only because the Pod was never created. **`PRODUCT_DEFECT-21`:** `infrastructure/kubernetes/overlays/local-freeswitch` re-includes `../../base/platform` and `../../base/runtime` directly while its `images:` block maps **only** `utcp-freeswitch`, omitting all eight image transformations and the ConfigMap/Secret generators that the canonical `overlays/local` (and its `runtime` sub-overlay, which also generates the Asterisk ARI credentials Secret and the `asterisk-local-sip-fixtures` `9900` ConfigMap) provides. `kubectl diff -k` against the live cluster reports **22 changed objects including 15 unrelated platform Deployments**, with 14 image reversions to unresolvable base placeholders — `utcp-local-registry:5000/utcp/api:0.1.0-k1-dev → utcp-api` (×11), `ghcr.io/kamailio/kamailio:5.8.6-bookworm → utcp-kamailio`, `utcp-local-registry:5000/utcp/rtpengine:0.1.0-k1-dev → utcp-rtpengine`, and `utcp-local-registry:5000/utcp/asterisk-ari:0.1.0-k1-dev → utcp-asterisk-ari`. Applying it would send the API, web, gateway, Reverb, scheduler, worker, the three telephony workers, the outbox dispatcher, the simulator event source, the ARI event bridge, **Kamailio** and **rtpengine** into `ImagePullBackOff` — destroying the very preconditions the parity proof exercises. Failed seam: **runtime selection**. The overlay was therefore **not applied**; the blocker is established deterministically from `kubectl diff` against the live cluster rather than by inducing a platform-wide outage. Consequently **not executed**: in-cluster security context and writable paths, FreeSWITCH startup/health in Kubernetes, Sofia profile and SIP listener in Kubernetes, runtime-selection projection, generic Service endpoints, Asterisk scale-to-zero, Playwright MCP natural login, Scenario A, Scenario B, the readiness marker, the local `fs_cli` hangup stimulus, selected-runtime-unavailable behavior, Asterisk zero-fallback counters and default-runtime restoration (never departed from). Static review otherwise records the parity design as sound and retained for the retry: the FreeSWITCH Deployment (`runAsNonRoot`, uid/gid `1000`, `fsGroup 1000`, caps drop ALL, `readOnlyRootFilesystem`, `RuntimeDefault`, no service-account token, ports sip UDP `5060` and rtp UDP `21000`, `envFrom` the generated ESL Secret, exec health probes, emptyDir at the three runtime paths), `freeswitch-sip` ClusterIP, the `application-runtime-sip` selector narrowed to `component=freeswitch` + `runtime-node=local-freeswitch-esl` + `runtime-selection=selected-application-runtime`, `asterisk-ari` `replicas: 0`, and `allow-freeswitch-sip-from-kamailio` (ingress UDP `5060` from kamailio-signaling and UDP `21000-21099` from rtpengine-media; egress UDP `5060` to kamailio-signaling, UDP `40000-40099` to rtpengine-media and UDP/TCP `53` to kube-dns) — with no NodePort, LoadBalancer, HostPort, HostNetwork, `ipBlock`, public SIP, public ESL, public RTP, TCP `8021` Service or policy, all-UDP or all-egress rule, dual runtime selector, Asterisk fallback selector, duplicated media route or prover modification, and Kamailio's generic `APPLICATION_RUNTIME_RELAY`/`APPLICATION_RUNTIME_UNAVAILABLE` routes carrying no provider-specific duplicate. The ESL fixture password is synthetic and clearly labelled, `secret-scan` passes, and the Event Socket binds `127.0.0.1:8021` with `apply-inbound-acl loopback.auto` and `stop-on-bind-error true` with no exposed surface. **Production is untouched**: the full-cluster Pod snapshot diff is empty with every UID and restart count preserved and all workloads Ready, no FreeSWITCH resource exists, `application-runtime-sip` is still absent, the live Kamailio ConfigMap still carries `ASTERISK_RELAY -> asterisk-sip`, live images are unchanged (`kamailio ghcr.io/kamailio/kamailio:5.8.6-bookworm`, `rtpengine …/rtpengine:0.1.0-k1-dev`, `api …/api:0.1.0-k1-dev`), `asterisk-ari` remains `replicas 1 ready 1` and selected, tables `41`, tenants `27`, RuntimeNodes `110` (asterisk, simulator), pending outbox `0`, Redis `sip`/`dialog`/`rtp`/`media` `0/0/0/0`, rtpengine sessions `0/0` with `ports_used 0`, Asterisk `0` active channels, and all production NetworkPolicy generations unchanged. One `PROOF_LIMITATION`: no committed check renders the parity overlay and asserts that all images resolve, which is why the full static suite passes against an overlay that would break the platform. The smallest bounded correction is to rebase `overlays/local-freeswitch` on `../local` instead of re-including `base/platform` and `base/runtime` — inheriting every image mapping and generator and reducing the parity delta to exactly the FreeSWITCH Deployment/Service, the `application-runtime-sip` selector narrowing, `asterisk-ari` `replicas: 0`, the ESL Secret and the generic Kamailio relay ConfigMap — plus a `freeswitch-config-check` guard asserting the rendered parity overlay resolves no bare `utcp-*` image name. `PRODUCT_DEFECT-17`, `-18`, `-19` = closed. `PRODUCT_DEFECT-20` = closed at image level, unverified in Kubernetes. `PRODUCT_DEFECT-21` = open. T3-S2A = Complete. T3-S2B = Complete. T3-S2C FreeSWITCH parity = INCOMPLETE. Provider-neutral signaling and media contract = proven against Asterisk only. Runtime agnosticism = not yet proven. T3-S2 overall = In Progress. T3-S3 external media edge = Not Started. T3 = In Progress. `UTCP_PHASE=T1`.
- **T3-S2C at `60bf137` (evidence `docs/evidence/t3/t3-s2c-baseline-reconciliation-and-freeswitch-auth-blocker.md`): `ENVIRONMENT_DRIFT-1` is RECONCILED and `PRODUCT_DEFECT-21` is CLOSED, but parity remains INCOMPLETE behind a new `PRODUCT_DEFECT-22` — FreeSWITCH answers the proxy-relayed INVITE with `407 Proxy Authentication Required`.** Drift was fully classified before any apply: `kubectl diff -k overlays/local` showed **5 changed objects / 151 lines**, all `STALE_CLUSTER_RESOURCE` with the repository authoritative — the `kamailio-config` ConfigMap still carrying `ASTERISK_RELAY -> asterisk-sip` instead of `APPLICATION_RUNTIME_RELAY -> application-runtime-sip`, the checksum-coupled `kamailio` Deployment (`0d788689… -> 9cb5e1f1…`, gen 18→19), the `asterisk-ari` Deployment missing the `utcp.dev/runtime-selection: selected-application-runtime` pod label (gen 16→17), the entirely absent `Service/utcp-runtime/application-runtime-sip`, and the `utcp-application-config` ConfigMap still on `APP_URL=http://gateway:8080` / `BROADCAST_CONNECTION=log` with all seven `REVERB_*` keys missing; items 1–4 originate from `e294ac7` and item 5 predates `ff3444f`. Two live-only workloads that `kubectl diff` cannot report — `utcp-runtime-fence-worker` (`components/runtime-fencing`) and `asterisk-ari-b` (`overlays/local-two-asterisk`) — were classified `PREVIOUS_PROOF_RESIDUE`, are committed in other overlays, are not pruned by apply, and `asterisk-ari-b` carries no `runtime-selection` label so it can never be a generic-Service endpoint. Nothing was `UNAUTHORIZED_MANUAL_DRIFT`, `CANONICAL_OVERLAY_DEFECT` or `UNCLEAR_AUTHORITY`. The historically dangerous `BROADCAST_CONNECTION` move to `reverb` was proven safe because the committed UI-D17 correction is present in the render — every PHP workload without the Reverb `secretRef` pins `BROADCAST_CONNECTION=log` (`asterisk-ari-events`, `kamailio-registration-observer`, `scheduler`, `simulator-event-source`, the three telephony workers, `utcp-migrate`) while every workload inheriting `reverb` has the `secretRef` (`api`, `control-plane-outbox-dispatcher`, `reverb`, `worker`) — and `utcp-migrate` already existed with `status.succeeded=1` and an identical spec, so the apply was a no-op for it with no seeder or destructive job. Reconciliation applied `overlays/local` and produced exactly two intended rollouts (Kamailio for the config checksum, Asterisk for the selection label); afterwards **`kubectl diff -k overlays/local` exits `0` with zero changed objects**, all pods Ready, `application-runtime-sip` resolving to `10.42.1.8` (Asterisk only), live Kamailio carrying `APPLICATION_RUNTIME_RELAY` ×2 and `ASTERISK_RELAY` ×0, rtpengine sessions `0/0` and `0` active channels. With the overlay rebased on `../local`, **`PRODUCT_DEFECT-21` is closed**: the parity diff is exactly its bounded five-object delta (`asterisk-ari` replicas `1→0`, new `freeswitch` Deployment, new ESL Secret, `application-runtime-sip` selector narrowing, new `freeswitch-sip` Service) with `kamailio-config` correctly absent because the generic relay is now the reconciled baseline. A first Scenario A attempt failed `application_runtime_unavailable` because `e294ac7` **also** amended two existing policies that had never been applied — `allow-kamailio-signaling-required-traffic` (egress UDP `5060` to freeswitch plus reciprocal ingress) and `allow-rtpengine-media` (egress UDP `21000-21099` to freeswitch plus reciprocal ingress); applying the committed security kustomization closed both, with nine other policies showing generation-only bumps (`EXPECTED_CONTROLLER_METADATA`). FreeSWITCH itself deployed correctly: Pod `freeswitch-77fbf94d74-vgtqk` ran the published digest `sha256:83c562fac3c9acd315fc623fe8f419a62ed811acb919b317d93081f4c3fed880` (built from `60bf137`) with `imageID` matching exactly, `uid=1000 gid=1000`, **zero restarts**, Ready on its committed exec health probes (confirming `PRODUCT_DEFECT-20` closed in Kubernetes), `utcp-internal` Sofia profile `RUNNING (0)` at `sip:mod_sofia@10.42.1.9:5060;transport=udp` with context `utcp`, PCMU in/out, rfc2833 DTMF, all three runtime directories writable, and the Event Socket bound `127.0.0.1:8021` with `apply-inbound-acl loopback.auto` and **zero** Services or NetworkPolicies referencing `8021`; `application-runtime-sip` resolved to `10.42.1.9` alone with `asterisk-ari` at `replicas 0`. An independent Playwright MCP natural login passed from the real HTTPS login page (dashboard reached, `/api/v1/auth/session` `200`, tenant `local` "Local Tenant" both statuses active, logout `200` then `401`) with `.playwright-mcp/` removed. **`PRODUCT_DEFECT-22`:** three unchanged-prover Scenario A runs all failed `timed out waiting for SIP message` after ~46 s. Kamailio logged `challenge` → `media_offer` → `media_delete`, and a bounded packet capture in the FreeSWITCH node network namespace filtered to UDP `5060` gives the decisive wire evidence — `10.42.2.176:5060 -> 10.42.1.9:5060 INVITE sip:9900@sip.utcp.local.test CSeq: 2 INVITE` ×3, `10.42.1.9:5060 -> 10.42.2.176:5060 SIP/2.0 407 Proxy Authentication Required` ×3, `ACK` ×3 — with FreeSWITCH counters `CALLS-IN 2 / FAILED-CALLS-IN 4` and `1 session(s) since startup`. The corridor therefore works and this is not a policy or routing failure: the selected runtime challenges the proxy-relayed INVITE even though `xml_locate configuration configuration name sofia.conf` confirms the running profile carries `<param name="auth-calls" value="false">`. Kamailio is a proxy relaying an INVITE it already authenticated with its own subscriber digest and holds no downstream credentials, so it can only ACK the `407`, after which the committed `failure_route[APPLICATION_RUNTIME_UNAVAILABLE]` runs `MEDIA_DELETE` and the browser — which waits only for `200 OK` — times out. Failed seam: **SDP offer / INVITE routing**. The profile declares `<domains><domain name="all" alias="true" parse="false"/></domains>` with no `apply-inbound-acl`, whereas the stock internal profile pairs `apply-inbound-acl` with `alias="false" parse="true"`. Consequently **not executed**: Scenario A media assertions, Scenario B and its readiness marker, the local `fs_cli` hangup stimulus, and selected-runtime-unavailable behavior; the Event Socket was used only for read-only `status`/`sofia status`/`xml_locate` plus one reversible `console loglevel` toggle that was restored. **Zero Asterisk fallback is proven**: `asterisk-ari` held `replicas 0` with no Pod for the entire parity window, `application-runtime-sip` resolved only to FreeSWITCH, one branch was issued and the failure surfaced through the canonical runtime-unavailable path with no reroute or dual delivery. One `PROOF_LIMITATION`: no committed check drives an inbound INVITE, so a profile that answers every call `407` still passes `freeswitch-config-check`, `freeswitch-overlay-check` and `freeswitch-startup-smoke-test` — the smoke test proves the profile reaches `RUNNING`, not that it accepts a call. Restoration applied `overlays/local` as the canonical authority (no manual patching), deleted the FreeSWITCH Deployment/Service/ESL Secret which the default overlay does not contain, and re-verified **`kubectl diff -k overlays/local` exit `0`** with `application-runtime-sip` back to Asterisk alone (`10.42.1.14`), `asterisk-ari` `replicas 1 ready 1`, all pods Ready; the committed FreeSWITCH NetworkPolicies and Kamailio/rtpengine corridor rules remain applied as canonical members of the security kustomization and are inert with no FreeSWITCH Pod. The workload delta versus the pre-reconciliation baseline is exactly the two intended reconciliation replacements (Kamailio and Asterisk), with every other UID and restart count preserved; tables `41`, tenants `27`, RuntimeNodes `110`, pending outbox `0`, Redis `sip`/`dialog`/`rtp`/`media` `0/0/0/0`, rtpengine sessions `0/0` with `ports_used 0`, Asterisk `0` active channels. The smallest bounded correction is to make the `utcp-internal` profile accept the Kamailio-relayed INVITE without a challenge (pair `apply-inbound-acl` with a bounded Kamailio-identity ACL and align the `<domains>` declaration), and to extend the FreeSWITCH smoke test to send one INVITE to `9900` and assert a `200 OK` with SDP rather than a `4xx`. `ENVIRONMENT_DRIFT-1` = reconciled. `PRODUCT_DEFECT-17` through `-21` = closed. `PRODUCT_DEFECT-22` = open. T3-S2A = Complete. T3-S2B = Complete. T3-S2C FreeSWITCH parity = INCOMPLETE. Provider-neutral signaling and media contract = proven against Asterisk only. Runtime agnosticism = not yet proven. T3-S2 overall = In Progress. T3-S3 external media edge = Not Started. T3 = In Progress. `UTCP_PHASE=T1`.
- **T3-S2C FreeSWITCH runtime parity at `04d06a9` (evidence `docs/evidence/t3/t3-s2c-freeswitch-runtime-parity-live-proof.md`) is COMPLETE: `PRODUCT_DEFECT-22` is closed and both committed scenarios pass live against FreeSWITCH with the unchanged committed WebRTC prover.** Canonical reconciliation to `04d06a9` was exactly the expected two-object delta — `kamailio-config` gaining `consume_credentials();` and the checksum-coupled `kamailio` Deployment (`9cb5e1f1… -> 89bdad9d…`, gen 19→20) — after which `kubectl diff -k overlays/local` exits `0` with zero changed objects. The running configuration places `consume_credentials()` **exactly once**, after `www_authorize()` and the identity-mismatch guard and before `add_contact_alias()`, `MEDIA_OFFER`, `record_route()` and `APPLICATION_RUNTIME_RELAY`, with **zero** occurrences in the REGISTER path, so failed authentication can never reach runtime relay and no downstream runtime credential or FreeSWITCH-specific auth retry exists. The FreeSWITCH image built from `04d06a9` published as digest `sha256:0c810f4b5e8caa600daff0c626d2961a2cb19ac3a3c3bcd7b3ffcd172974219c` (configuration digest `89ecc33d8f6413b3…`, uid/gid `1000:1000`) and the running Pod's `imageID` matched it exactly with `restarts 0`, Ready on its committed exec probes, `utcp-internal` bound `sip:mod_sofia@10.42.1.15:5060;transport=udp` in context `utcp`; the parity diff stayed bounded to its five documented objects. **The authentication cutoff is proven on the wire** by a header-name-only capture in the FreeSWITCH node network namespace (no credential values recorded): every `INVITE`, `ACK` and `BYE` toward FreeSWITCH shows `auth_headers=NONE`, and FreeSWITCH answers `100 Trying` then `200 OK` — the `407 Proxy Authentication Required` that blocked the previous attempt is gone, with no `401` or `403` either. **Scenario A** (`browser-originated-bye`) exited `0` with `errors []`, Call-ID `d2de2e809d08a8@utcp-t3-media-prover`: one INVITE branch reached FreeSWITCH (channel `0a8fe80b-…`, `sofia/utcp-internal/ts-…`, dest `9900`, app `echo`, context `utcp`, PCMU both ways, `CS_EXECUTE`), ACK and BYE both targeted the 2xx `Contact` remote target `sip:9900@10.42.1.15:5060;transport=udp`, ICE gathering `complete` with both candidates host/private, DTLS `connected`, outbound `179 p / 28640 b` against inbound `178 p / 28480 b`, `audioEnergy 0.11166849224328824` from `inbound-rtp.totalAudioEnergy`, jitter `0.002`, `packetsLost 0`; rtpengine per-leg shows the browser leg `10.42.0.179:40011 <> 10.42.1.17:40264` (`RTP/SAVPF`, in 186 / out 180) and the runtime leg `10.42.0.179:40046 <> 10.42.1.15:21018` (`RTP/AVP`, in 178 / out 184) plus RTCP on `:21019` — **inside the committed `21000-21099` range with return traffic on rtpengine's `40000-40099`** — and the browser BYE received `SIP/2.0 200 OK` with the media session and ports released and FreeSWITCH channels back to `0 total`. **Scenario B** (`runtime-originated-bye`) exited `0` with `errors []`, Call-ID `056b1e5ac11d5@utcp-t3-media-prover`: the committed stdout marker `UTCP_T3_MEDIA_PROVER_READY_FOR_RUNTIME_HANGUP` appeared **exactly once** while the Job was still active and after all media assertions, the bounded stimulus used only loopback `fs_cli -H 127.0.0.1 -P 8021 -x 'uuid_kill <uuid>'` against exactly one channel selected from a zero-channel baseline (`ac41d4a1-…`, dest `9900`, app `echo`, `CS_EXECUTE`), and the resulting FreeSWITCH BYE carried the proven alias corridor (`;alias=10.42.0.180~35398~5;transport=ws`) to the browser, which matched its own dialog and answered `200 OK` back to FreeSWITCH; media was `180/28800` out against `178/28480` in with `audioEnergy 0.1122658945856622`, per-leg `10.42.0.179:40068 <> 10.42.1.18:33220` (SAVPF) and `10.42.0.179:40080 <> 10.42.1.15:21006` (AVP) plus RTCP `:21007`, and `MEDIA_DELETE` returned sessions and ports to zero with no retransmission or timeout. **Selected-runtime unavailability** was proven by scaling FreeSWITCH to zero (reversible canonical action) until `application-runtime-sip` had zero Ready endpoints while Asterisk stayed at zero replicas: one authenticated SDP INVITE produced exactly `challenge` → `media_offer` → `media_delete` → `kamailio_application_dialog_rejected result=application_runtime_unavailable`, with no INVITE delivered to Asterisk, no second runtime, no dual branch, no direct-media fallback and zero residual sessions or allocations; FreeSWITCH was then restored through the committed parity overlay. **Zero Asterisk fallback is runtime-proven**: `asterisk-ari` held `replicas 0` with literally no Pod ("No resources found in utcp-runtime namespace") for the entire parity window, so `Asterisk INVITEs = 0`, `active channels = 0` and `media packets = 0`, and `application-runtime-sip` resolved to FreeSWITCH only. Provider neutrality holds — the same unchanged prover binary and the same generic Kamailio routes (`APPLICATION_RUNTIME_RELAY`, `APPLICATION_RUNTIME_UNAVAILABLE`, `MEDIA_OFFER`, `MEDIA_ANSWER`, `MEDIA_DELETE`, `APPLICATION_RUNTIME_MEDIA_REPLY`) served both Asterisk in T3-S2B and FreeSWITCH here with no provider-specific duplicate, and no FreeSWITCH Pod IP, channel UUID, `fs_cli`, ESL or FreeSWITCH-specific SDP marker appears in browser logic. Containment is intact: no NodePort, LoadBalancer, HostPort, HostNetwork, public SIP, public ESL, public RTP, public UDPRoute, `ipBlock`, all-UDP or all-egress rule, dual selector or Asterisk fallback selector, and **zero** Services or NetworkPolicies referencing TCP `8021`. The default runtime was restored through `overlays/local` (not manual patching) with FreeSWITCH Deployment/Service/ESL Secret deleted, `asterisk-ari` back to `replicas 1 ready 1` as the sole `application-runtime-sip` endpoint (`10.42.1.21`), and a **final `kubectl diff -k overlays/local` exit `0`**. The workload delta versus the pre-proof baseline is exactly the two intended rollouts (Kamailio for the `consume_credentials` checksum, Asterisk for the parity scale back to 1) with every other UID and restart count preserved; tables `41`, tenants `27`, RuntimeNodes `110` (asterisk, simulator), pending outbox `0`, Redis `sip`/`dialog`/`rtp`/`media` `0/0/0/0`, rtpengine sessions `0` and `ports_used 0`, Asterisk `0` active channels. All proof resources, image tags, node caches, scratch results, temporary Helm and `.playwright-mcp/` were removed and no credential material remains. One `EXPECTED_BEHAVIOR` note: a single Scenario A run failed on stale credentials because the scratchpad issuance suffix collided with an earlier proof user under `users_normalized_email_unique`; a fresh unique suffix resolved it, and no product behaviour was involved. `PRODUCT_DEFECT-17` through `PRODUCT_DEFECT-22` = closed. T3-S2A = Complete. T3-S2B = Complete. T3-S2C FreeSWITCH parity = **Complete**. T3-S2 overall = **Complete**. Provider-neutral application-runtime contract = **proven against Asterisk and FreeSWITCH**. External browser media readiness = not claimed. T3-S3 external media edge = Not Started. T3 = In Progress. `UTCP_PHASE=T1`.
- **T3-S3 external media-edge architecture decision at `803b819` (evidence `docs/evidence/t3/t3-s3-external-media-edge-architecture.md`): architecture selected, awaiting bounded implementation. No Kubernetes resource was applied and the cluster is unchanged.** The exact external failure is isolated as *"the browser receives a candidate that is valid inside the cluster but unreachable from the external browser network"*: rtpengine advertises its Pod IP inside `10.42.0.0/16` while the host's `ip route get 10.42.0.179` resolves via the default gateway `192.168.86.1`, and the host routes only to the k3d node network `172.24.0.0/16`. NetworkPolicy denial, rtpengine port-range denial, runtime RTP denial, codec mismatch, ICE-credential failure, DTLS failure and SIP/SDP failure are each excluded by the already-completed T3-S2B and T3-S2C proofs, so no media scenario was rerun. **Selected canonical contract** (provider-neutral, one declaration per field): `UTCP_PUBLIC_MEDIA_ADDRESS` in `versions.env` as the sole public-address authority, projected into rtpengine's **already-existing** advertised-address seam — the pinned `mr26.0.1.19` binary documents `-i, --interface=[NAME/]IP[!IP]` and the entrypoint currently passes `internal/${POD_IP}!${POD_IP}`, so only the advertised half changes; the public UDP range reuses the existing `RTPENGINE_MEDIA_PORT_MIN/MAX` with no second declaration; the internal endpoint stays the downward-API Pod IP; and readiness must prove actual reachability rather than process health. **Local k3d projection**: publish `127.0.0.1:40000-40099/udp@server:0` in `infrastructure/k3d/cluster.yaml` (mirroring the existing `127.0.0.1:443` edge so the advertised address survives Pod restarts, node-IP shuffles and cluster stop/start), add `--kube-apiserver-arg=service-node-port-range=30000-40099`, add a `rtpengine-media` NodePort Service with 100 UDP entries where `nodePort == port == targetPort`, and pin rtpengine by `nodeSelector` to the published node — requiring cluster recreation via the established `k3d-create`/`k3d-recreate-proof` path. **Future deployment projection**: identical contract fields with the projection swapped for a cloud UDP load balancer or dedicated media node pool; no cloud integration is implemented now. **Models rejected on measured evidence, not principle**: Gateway API/Traefik UDP because the SHA-pinned Gateway API `v1.5.1` standard channel installs **no `udproutes` CRD** (confirmed live) and Traefik's running entry points are `web/websecure/traefik/metrics`, all `/tcp`; LoadBalancer because the k3s klipper-lb implementation materialises **one container per Service port** (observed `lb-tcp-80`, `lb-tcp-443`), so a 100-port media Service would mean 100 containers per node, and its external addresses are shuffling node IPs; dedicated media edge / `hostNetwork` because PSA `restricted:v1.35` on `utcp-platform` **forbids both `hostNetwork` and `hostPort`**, proven by non-mutating server dry-run (`violates PodSecurity "restricted:v1.35": hostPort` and `host namespaces (hostNetwork=true)`), so adopting it would require weakening the PSA contract T5-A78 established; NodePort **alone** because the advertised candidate would then be a node IP and node IPs demonstrably shuffle on host restart; and manual `socat`/`iptables`/`nftables` DNAT because the repository establishes none of them as managed infrastructure and all need recurring manual commands or a second management plane. `Service.spec.ports` is `[]ServicePort` with scalar `port`/`nodePort` and **no range field** (`kubectl explain`), so 100 enumerated entries are unavoidable for any Service-based model; k3d `v5.9.0` does support published ranges (docs example `30000-32767:30000-32767@server:0`) but warns that *"Docker creates iptable entries and a new proxy process per port-mapping"*, so the 100-port publication cost must be measured during implementation. ADR-020 already names this direction — *"browser-reachable media needs a later slice that publishes a host UDP range and extends this ADR"* — and must be extended to record the canonical advertised address, the publication, the NodePort media Service, node pinning, and the re-evaluation of its own `hostNetwork` and `NodePort` rejections on PSA evidence; two guards need bounded amendment with the precedent in `t3-rtp-media-preparation-audit.md`: `scripts/k3d/config-check:45` (rejects `rtp|rtpengine|NodePort` in `cluster.yaml`) and `scripts/k3d/verify:106` (fails on serverlb `/udp`, `:400xx->`, `NodePort`, `rtpengine`). Containment is preserved by construction: public RTP is exactly `40000-40099`, rtpengine ng `2223` and metrics `2224` stay ClusterIP/in-cluster, Asterisk `10000-20000` and FreeSWITCH `21000-21099` stay in-cluster, no ESL/ARI/AMI is public, no Pod-CIDR route is added, no `hostNetwork`/`hostPort` is used, and absent `UTCP_PUBLIC_MEDIA_ADDRESS` the default remains internal-only with the Pod IP advertised exactly as today. Fifteen static validations and eighteen mutation cases are specified, along with the full external acceptance proof (natural host-browser login, candidate equal to the public address and inside `40000-40099` and never a Pod/Service/runtime IP, real external UDP reaching rtpengine with echo and positive increasing `inbound-rtp.totalAudioEnergy`, both BYE directions deleting media and releasing ports, four observable failure modes with no private-candidate fallback, and the containment proof). **T3-S3 is split into T3-S3A** (external-media authority and local projection) **and T3-S3B** (external browser media proof and failure behaviour) because cluster recreation and an external host-browser harness form a genuine implementation/proof boundary; T3-S3 remains separate from conference admission, external trunks, PSTN, campaigns, agent state and durable call control. Decision standard: `EVIDENCE_SUFFICIENT_FOR_BOUNDED_IMPLEMENTATION`; no operator architecture decision remains. Next AI coder = **Codex** for T3-S3A. T3-S2 = Complete. T3-S3 = architecture selected and awaiting bounded implementation. T3 = In Progress. External browser media readiness = not claimed. `UTCP_PHASE=T1`.
- **T3-S3A external media projection at `9b253f0` (evidence `docs/evidence/t3/t3-s3a-external-media-projection.md`) is implemented in the repository and awaiting cluster recreation and live validation.** The default local environment remains internal-only; the dedicated projection owns `UTCP_PUBLIC_MEDIA_ADDRESS=127.0.0.1`, publishes only UDP `40000-40099` through the media-edge k3d profile and matching 100-port `rtpengine-media` NodePort with `externalTrafficPolicy: Local`, and pins rtpengine to `utcp.dev/media-edge=true`. Offline rendered-image/generator preservation, bounded-delta, k3d restriction, and mutation checks pass. No Kubernetes resource was applied, the cluster was not recreated, and T3-S3B remains Not Started. T3-S2 is Complete, T3 remains In Progress, and `UTCP_PHASE=T1` is unchanged.
