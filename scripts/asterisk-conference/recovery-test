#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$ROOT/apps/api"

php artisan test \
  tests/Feature/Asterisk/AsteriskConferenceRecoveryTest.php \
  tests/Feature/Asterisk/AsteriskAriAdapterTest.php
